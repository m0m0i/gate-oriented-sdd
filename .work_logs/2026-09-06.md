## 2026-09-06 — m0m0i

### Summary

The Japanese README's Status section is written as Japanese now, not as a clause-by-clause mirror of the English (#90). Same fifteen claims; the reviewer read both languages and confirmed it.

### Activities

- `90-ja-status-in-natural-japanese` (#90): T1–T2, five commits, one file outside the spec directory. Entered as acknowledged unplanned work at the author's request, out loud, rather than through `sprint`.
- Reviewer, by subagent: CLEAN with 2 LOW, 1 INFO → fixed → CLEAN with nothing outstanding. The first LOW was a translation nuance worth having: "greenfield" had become an empty repository, where the repo means one with no harness footprint.

### Decisions

- **Paragraphs and ですます調, no bullet lists.** The rest of `README.ja.md` has no list at all; a natural rewrite that introduced them would have read as a different document.
- **The bold labels became sentence openers**, the file's dominant pattern for emphasis; the label text is unchanged.
- **The reviewer's C-3 pass was done independently of the claim map**, at its own initiative, and is the reason the run was worth a review round: the map is the author's; the pass is not.
- **Accepted rather than fixed:** nothing; the reviewer's one polish note was applied after the receipt.

### Next steps

- [ ] A new iteration: `backlog` re-groom, then `sprint`. #81 first among the findings.

## 2026-09-06 — m0m0i (second entry: #92's branch)

### Summary

Every place that called the eval suite "unrun" now calls it under development, with the fact kept in the same sentence: cases authored, command in early access here, no case has run (#92). The author's wording; the honesty stays.

### Activities

- `92-eval-suite-under-development` (#92): T1–T2 and two review rounds, seven commits, eight files — both READMEs, `docs/layout.md`, `.steering/structure.md`, `evals/README.md`, `AGENTS.md`, and, after an AC3 amendment landed alone, one line each in `docs/NORTH_STAR.md` and `docs/DESIGN.md` that had quoted the old label.
- Reviewer, by subagent: BLOCKED with 1 HIGH, 2 MEDIUM, 1 LOW → fixed → CLEAN with 1 LOW, closed by recording today's `--help` render.
- The eval smoke run was repeated today before the wording change: CLI 2.1.263, "in early access", exit **1** where 2026-09-05's exited 0. Recorded on the branch; it is why "exits 0" left the README sentence.
- Filed 93: `implement` says `Status: done` flips both before and after the review; the reviewers have noted it on every branch this week.

### Decisions

- **"Under development" leads; the run fact follows in the same sentence.** The label alone would have been the edit `NORTH_STAR.md` calls the most tempting in the repository; keeping "no case has run" in all six files is what makes it not that.
- **The two quoted lines were fixed rather than left false with a follow-up**, by widening AC3 in its own commit first (C-8).
- **The dropped "exits 0" clause is deliberate and recorded:** at exit 1 there is no exit code that could be read as a pass, so the worked example lost its object; the refusal that remains still has one.
- **A zsh slip cost a commit:** an unquoted variable holding six file names was one argument to grep and to `git add`, so the greps found nothing and the edits were not staged. Arrays from now on.
- **Accepted rather than fixed:** nothing; the last LOW was closed by a one-line record addition.

## 2026-09-06 — m0m0i (third entry: #95's branch)

### Summary

"So none has run yet" and its Japanese counterpart leave the eval sentence (#95). The fact stays, twice, in both languages.

### Activities

- `95-drop-redundant-clause` (#95): one clause out of each README, four commits. Reviewer, by subagent: CLEAN on the first pass, one INFO accepted.

### Decisions

- **Spawned rather than inline for a two-line change**, because the README's "a receipt on every spec since #17, spawned on all but two" would have become "all but three".
