## 2026-09-05 — m0m0i

### Summary

Ran `contract` and `design-doc` on this repository (#56), the second half of the inception run: two documents, six ADRs, two rules compiled into the dogfood rulebook, four issues filed, and a `verified.md` section that records what did not join. Settled how the remaining three skills get tested, and found `claude plugin eval` still gated.

### Activities

- `56-run-contract-design-doc` (#56): T1–T6, ten commits, 15 files. `docs/CONTRACT.md` (37 rows — 14 Mechanical, 18 Judgment, 5 Narrative), `docs/DESIGN.md`, `docs/decisions/ADR-1…6`, `C-8` and `C-9` appended to `claims-and-prose.md`, `tech.md` +4/−0, `structure.md` +5/−2, `verified.md` +33. Nine validators and 54/0 at both ends; no shipped path changed, so no version bump.
- Reviewer, by subagent both times: CLEAN with 1 MEDIUM, 3 LOW, 2 INFO → the four fixed in `41b3f1f` → CLEAN with 1 LOW, 1 INFO. The MEDIUM was `DESIGN.md` naming `hooks/hooks.json` and `hooks.json`, copied from `AGENTS.md:23`; neither exists.
- Filed #69 (`check-locks.py --update` prints a success line naming a directory it verified nothing in), #70 (`contract` has no vocabulary for an existing rulebook; step 6 dictates a provenance claim that was false here), #71 (`design-doc` regenerates a live `structure.md` by instruction; the reviewer clause it says escalates to an ADR does not mention one), #72 (two paragraphs cite closed #16 as the reason there is no dogfood lock; `AGENTS.md:23`'s hook paths). Comments on #22 and #58.
- Eval smoke run before the spec: on Claude Code 2.1.252 `claude plugin eval --help` renders every option, and any invocation prints "`plugin eval` is currently in early access" and exits 0 — nothing runs, no results directory, no cost. The README's "never run" stays true. Recorded in `observations.md` T4; the exit-0 no-op belongs in the README update.

### Decisions

- **The test queue, agreed with the user.** `sprint` runs for real on this tracker after #58's re-groom, because the issues it makes are real work and a throwaway has no backlog to decompose. `init` runs on a scratch clone of a real, unrelated project with the harness stripped out, so detection meets a toolchain that was not written for it; which project is the user's call and is not recorded here. Evals wait for the account. The README Status update goes last, under #48, once the runs it describes are done.
- **Three protocol decisions taken before the run, not inside it** (spec clarifications 1–3): `CONTRACT.md` cites `CONTRIBUTING.md` rather than restating it; `tech.md` states the true provenance and the deviation from step 6 is recorded; the rulebook diff lands as the skill wrote it. Rejected: strict no-intervention on the provenance line, because `tech.md` is read by both gates and every session, which is what made #55's equivalent choice safe there and not here.
- **Six ADRs from roughly twelve candidates.** Kept where a rejected alternative and a disliked consequence were both already on record. Rejected: one per documented decision, which the skill's own red flag calls notes.
- **`structure.md` reconciled, not rewritten**, under an AC8 amendment landed alone ahead of T3 (#59's convention, now `C-8`). The reconciliation found the `docs/` row false since #55.
- **`C-8` and `C-9` answer #59 and #55's reviewer finding.** `C-8` states the amendment convention where the reviewer reads it; whether that closes #59 is the user's call at merge, since #59 may want it in `CONTRIBUTING.md` too.
- **Accepted rather than fixed, so the tree stays at the reviewed sha, as #55 did with its three LOWs:** the "T6 capture, verbatim" heading over an excerpt (LOW, C-1) — the capture was taken at `34dbd9b`, which is the fact the omitted line carried. INFO: `C-8`'s check clause reads "changes an `AC` line" and so matches a tick; the rule means an amendment to a criterion's text. Tighten on the next branch that touches the rulebook.
- **`baseline.sh` lives in the spec directory.** Rejected: `scripts/`, which the spec puts out of scope and #22 owns.

### Corrections to earlier claims

- The first push of this entry named two private repositories in the bullet above, and CI's leakage guard caught it. Rewritten rather than scrubbed, per the clean-room rule. The local gate did not fire because `.work_logs/` is outside `Source globs`, so the validators did not run on the commit that added the entry; a work-log entry written after the review needs `check-leakage.sh` run by hand before it is pushed.
- The first draft of #56's spec, and a message earlier in this session, counted five skills in the per-issue chain. `README.md:63` counts four; `archive` is not in it. `observations.md` T4 says four.

### Next steps

- [ ] Merge the PR for #56. It closes #56; say whether it closes #59.
- [ ] #58 — `backlog`, next in the queue, with `DESIGN.md`'s risk list to groom against.
- [ ] File the `sprint` and `init` issues; run `sprint` on the re-groomed backlog, `init` on the fresh clone.
- [ ] #48 — the README update, carrying `observations.md` T4's rows: version, "skills never executed", "no reviewer has reviewed a real diff", the Japanese mistranslation, `layout.md`'s `docs/` picture, and the eval no-op.
- [ ] #72 and #69 are each one small PR; #70 and #71 are skill edits with version bumps.

## 2026-09-05 — m0m0i (second entry: #58's branch)

### Summary

Ran `backlog` on this repository (#58), the third skill in the test queue and the first grooming with epics to read. Fourteen rows, nineteen open issues each once, two rows of work that has no issue yet, and one skill defect filed.

### Activities

- `58-regroom-the-backlog` (#58): T1–T5, eight commits, five files. `docs/BACKLOG.md` re-groomed — preamble true again, `Epic` column added, five pairs of issues as single rows, the test-queue runs at row 1; `docs/verified.md` gains an eight-question section. Nine validators and 54/0 at both ends; the same twenty open issues before and after; nothing outside `BACKLOG.md`, `verified.md` and the spec directory.
- Reviewer, by subagent both times: CLEAN with 2 MEDIUM, 3 LOW, 2 INFO → all five fixed in `79671d5` → CLEAN with 2 INFO. The MEDIUMs were a count (row 13 said seven `—` rows; four) and a `verified.md` row committed one task before the observation it reports.
- Filed #74 — `backlog` names a walking skeleton that may not exist, two sections ("Next up", "Not ready") its template does not define, and a `Blocks` column form that reads as issue numbers.

### Decisions

- **The test-queue runs lead, and #26 is displaced a third time — by decision, and the row says so.** The skills were untested and each run has filed three or four defects; the user chose to finish the queue first. Rejected: EPIC-1 first as `EPICS.md` suggests, which would have put the product's live falsifier ahead of work already in flight.
- **Pairs as single rows** — #26+#36, #19+#69, #35+#25, #14+#18, #70+#71. The skill asks for coarse items and red-flags issue-shaped rows; the pairs are the work that would be specced together anyway. Rejected: one issue per row, which is the inversion the preamble apologises for.
- **Two rows with no issue** — the `sprint` and `init` runs, and the `epics` re-run — so `sprint` has something to decompose. Rejected: filing them now, which is `sprint`'s job and the seam the next run exists to observe.
- **`EPICS.md` untouched.** #69 and #72 sit in rows under epics that do not list them, recorded for the re-run rather than fixed by editing a document `backlog` only reads.
- **The pause produced zero changes.** Recorded, as #55 and #56 did: accepted-as-recommended cannot be told from right.
- **Accepted rather than fixed:** both INFOs from the second review — T5's "re-run at the final tree" was observed one docs-only commit before the review fix, and the pasted T5 block predates the parser's new duplicate line. Both true of the current tree; the reviewer re-ran everything at HEAD.

### Next steps

- [ ] Merge the PR for #58.
- [ ] `sprint` on the groomed backlog: rows 1 and 13 are what it decomposes; observe what it does with rows that are already issues.
- [ ] `init` on a scratch clone of a real, unrelated project, with the harness stripped.
- [ ] #48 — the README update, last, carrying the false-statement rows from #56's and #58's rereads.
- [ ] #74 is a skill edit with a version bump; it can ride with #70 and #71.

## 2026-09-05 — m0m0i (third entry: the sprint run, and #77's branch)

### Summary

Ran `sprint` on the groomed backlog, rows 1 and 2 — the fourth skill in the test queue and the one that writes no file — then recorded it (#77). Three issues created, one re-scoped, one milestone, one defect filed for the seam between `sprint` and `backlog`.

### Activities

- `sprint`, run directly: #76 (`init` on a scratch clone), #77 (this record), #78 (the four "nothing cites" statements) created from rows 1 and 2; #48 re-scoped by comment to own the whole README Status rewrite; milestone 1 "Iteration 1 — the test queue" holding those three plus #48, #72, #47. Open issues 20 → 23.
- `77-record-the-sprint-run` (#77): T1–T3, six commits, three files. The run's notes verbatim, an eight-row `verified.md` section, and #79 filed after the record landed. Open issues 24.
- Reviewer, by subagent both times: CLEAN with 1 MEDIUM, 3 LOW, 1 INFO → the four fixed in `80a9f2a` → CLEAN with 1 INFO.

### Decisions

- **`sprint` ran directly, with no issue of its own.** The spec skill says no issue, no spec, and that the user decides out loud how work without one enters; the user chose the skill's own model — issues come from `sprint` — over filing one by hand to test it. The record then went through an issue `sprint` itself created.
- **The iteration is rows 1 and 2.** Row 13, the `epics` re-run, stays in the backlog: taking it would skip rows 3–12, which the skill says is a re-groom, not a selection. Rejected: rows 1, 2 and 13, which the queue wanted.
- **The before list of twenty issue numbers stays in `observations.md`**, and the `verified.md` row says why: a list in a cell is noise, and the after list is the row.
- **Accepted rather than fixed:** the INFO that three facts in the section — #79's existence, the Claude Code version, `gh`'s template behaviour — are outside the reviewer's allow-list and rest on the record.
- **#76 will clone the plugin's first real consumer.** A Python project with a harness install already present to strip, so `init` meets a real toolchain and the migration-adjacent path at once. Its name stays out of this repository; #73's first CI run is why.

### Next steps

- [ ] Merge the PR for #77.
- [ ] #76 — spec, then the `init` run on the scratch clone; `check-leakage.sh` by hand before every push.
- [ ] #78, then #48 last, with #72 and #47 riding the same milestone.
- [ ] The next `backlog` grooming records the `sprint` trigger as partly fired and corrects rows 1 and 2, or #79 lands first and `sprint` does it.
