# Work log — 2026-09-21

## 2026-09-21 — m0m0i

### Summary

Filed the two follow-ups #182's review surfaced, and took the bounded backlog correction that filing them forced. No code changed; #182's CLEAN receipt at `af809f2` is untouched, because none of this is on the `- Source globs:` line.

### Activities

- **Filed #184** — hand-written `:NNN` citations across `scripts/test-gates.sh` that do not resolve. Verified all six before filing rather than taking the review's word: `:45`, `:1174`, `:1474`, `:1514`, `:1832` and `:2069` are all stale today, and the three checked against `main` were stale *there* too — `:1174` cites `:372, :384, :436` on both, while the real uses were `:503, :517, :717, :729, :781, :846` on `main` and are `:827, :841, :1041, :1053, :1105, :1170` now. So #182's 321-line insertion widened the gap without causing it.
- **Filed #185** — the reviewer cannot verify a mutation record. Its bash policy is an allow-list ending in "Nothing else" (`.claude/agents/gate-sdd-reviewer.md:40`), so mutating a copy of the tree is outside it, and on #182 it said so as a finding rather than working around it.
- **`check-backlog-tracker.py` went red the moment both issues existed**, and reported a third thing nobody had noticed: row 1's `Item` cell still cited **#79**, which PR #183 closed on 2026-09-20. That failure was already waiting for the next pull request regardless of this session — #182's own entry predicted it yesterday in as many words.
- Corrected `docs/BACKLOG.md`: row 1 cites **#174** alone at ~1 issue with #79's citation moved into the reasoning, and #184 and #185 joined `## Open, not planned` with reasons and falsifiers. Check now reads `47 open issue(s) against 24 row(s), no drift`.

### Decisions

- **Corrected the backlog rather than leaving CI red, and kept it to what the check asked for.** Placing #184 and #185 as *rows* would be a product judgment and belongs to `backlog`; `## Open, not planned` is the section #79's AC4 built for exactly this — open, deliberately not a row, with the condition that would make it one. The row-1 fix is the citation move the check's own message prescribes. **A real grooming is still owed** and this is not it: 47 open issues against 24 rows is the eighth grooming's problem, not this session's.
- **Yesterday's entry says these two were "Not filed — filing it is the user's call."** That was true when written and is now wrong; it is corrected here rather than edited there, per the work log's append rule. The user asked for both on 2026-09-21.
- **#184 filed as `task`, #185 as `enhancement`.** #184 is mechanical with a real "what must not change" (no case changes verdict, comment-only diff). #185 is a capability question with genuinely open design — a mutation registry may only move the unverified claim from a comment into a registry entry, which is on the issue as an open question rather than answered.

### Next steps

- [ ] **Open #182's pull request.** Branch `182-squash-merge-defeats-the-merged-skip` at 0.17.0, CLEAN receipt, all thirteen validators and `check-backlog-tracker.py` green. It is the only thing left in that flow.
- [ ] **An eighth grooming is owed.** 47 open issues against 24 rows, and this session touched the list only where a guard failed.
- [ ] **`.specs/` holds two live specs.** An `archive` sweep is owed, on request, and is not part of any flow.
