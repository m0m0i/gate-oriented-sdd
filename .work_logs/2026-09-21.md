# Work log — 2026-09-21

## 2026-09-21 — m0m0i

### Summary

Filed the two follow-ups #182's review surfaced, and took the bounded backlog correction that filing them forced. No code changed; #182's CLEAN receipt at `af809f2` is untouched, because none of this is on the `- Source globs:` line.

### Activities

- **Filed #184** — hand-written `:NNN` citations across `scripts/test-gates.sh` that do not resolve. Verified all six before filing rather than taking the review's word: `:45`, `:1174`, `:1474`, `:1514`, `:1832` and `:2069` are all stale today, and the three checked against `main` were stale *there* too — `:1174` cites `:372, :384, :436` on both, while the real uses were `:503, :517, :717, :729, :781, :846` on `main` and are `:827, :841, :1041, :1053, :1105, :1170` now. So #182's 321-line insertion widened the gap without causing it.
- **Filed #185** — the reviewer cannot verify a mutation record. Its bash policy is an allow-list ending in "Nothing else" (`.claude/agents/gate-sdd-reviewer.md:40`), so mutating a copy of the tree is outside it, and on #182 it said so as a finding rather than working around it.
- **`check-backlog-tracker.py` went red the moment both issues existed**, and reported a third thing nobody had noticed: row 1's `Item` cell still cited **#79**, which PR #183 closed on 2026-09-20. That failure was already waiting for the next pull request regardless of this session — #182's own entry predicted it yesterday in as many words.
- Corrected `docs/BACKLOG.md`: row 1 cites **#174** alone at ~1 issue with #79's citation moved into the reasoning, and #184 and #185 joined `## Open, not planned` with reasons and falsifiers. Check now reads `47 open issue(s) against 24 row(s), no drift`.

### Decisions

- **Corrected the backlog rather than leaving CI red, and kept it to what the check asked for.** Placing #184 and #185 as *rows* would be a product judgment and belongs to `backlog`; `## Open, not planned` is the section #79's AC4 built for exactly this — open, deliberately not a row, with the condition that would make it one. The row-1 fix is the citation move the check's own message prescribes. **A real grooming is still owed** and this is not it: 47 open issues against 24 rows is the eighth grooming's problem, not this session's.
- **Yesterday's entry says these two were "Not filed — filing it is the user's call."** That was true when written and is now wrong; it is corrected here rather than edited there, per the work log's append rule. The user asked for both on 2026-09-21.
- **#184 filed as `task`, #185 as `enhancement`.** #184 is mechanical with a real "what must not change" (no case changes verdict, comment-only diff). #185 is a capability question with genuinely open design — a mutation registry may only move the unverified claim from a comment into a registry entry, which is on the issue as an open question rather than answered.

### Next steps

- [ ] **Open #182's pull request.** Branch `182-squash-merge-defeats-the-merged-skip` at 0.17.0, CLEAN receipt, all thirteen validators and `check-backlog-tracker.py` green. It is the only thing left in that flow.
- [ ] **An eighth grooming is owed.** 47 open issues against 24 rows, and this session touched the list only where a guard failed.
- [ ] **`.specs/` holds two live specs.** An `archive` sweep is owed, on request, and is not part of any flow.

## 2026-09-21 — m0m0i

### Summary

#174 built, reviewed three rounds, and released as 0.18.0. The ordered list now has one place to write down a row that left, and the guard's own remedy points at it.

### Activities

- `174-a-discharged-row-has-no-home` (#174): spec, clarify, five tasks, three review rounds, receipt, bump. Branch open, PR not yet raised.
  - **Investigation changed the issue before any task ran.** Three of its five acceptance criteria did not survive contact. AC4 asks to narrow the skill's `What changed` guidance and there is none — the section is this repository's own invention, defined in no template, so the document runs ahead of the skill it is templated from, which is #74's defect in the opposite direction. AC2's prune condition ("after the archived specs have absorbed the detail") is a prune with a manual precondition, because `archive` is a sweep on request. AC5 is #164's AC6 word for word. And the issue's four places for a discharge trace are five: the 2026-09-20 between-grooming correction became a preamble bullet, because there was nowhere else — the first correction `check-backlog-tracker.py` ever prompted landed in the first thing every reader of the list sees.
  - **`clarify`, four questions, all four recommendations taken.** The one that changed the shape: the guard's finished-direction remedy is the section's mechanical reader, so this is not the prose-only patch the issue's footer predicted.
  - **T1 (Red/Green):** case 145 in `test-gates.sh`, eight assertions, then the remedy in `check-backlog-tracker.py`. AC6's halves were red before the change; AC7's passed on first run, so red-capability came from the named mutation recorded on the case — wiring the discharge heading into the absent direction's `cited` region flips both halves and the run reports `no drift`. Applied, observed, reverted.
  - **T2, T3:** the two template sections, the bounded between-grooming append, step 6, and the two rules that named sections the template does not define.
  - **T4:** the section migrated into `docs/BACKLOG.md`, then the guard run by hand against the real document. Red — for #188, which predates the branch.
  - **T5:** `docs/verified.md:170` discharged in place; it recorded "Next up"/"Not ready" as being in no template, true of the 0.4.2 run it dates itself to and falsified by T3.
  - 145 cases pass, thirteen validators green, `claude plugin validate --strict` clean.

### Decisions

- **The guard's remedy is where the new section gets a mechanical reader.** Its message for a closed `Item` citation said *"move the citation into `Why here`"* — place 1 of the scatter, and the one place that does not exist when a row ships whole. Rules out the prose-only reading of this issue, and answers CAP-6 for this change rather than recording it as a known gap.
- **The section is bullets, never a table, and that is a rule rather than a structure.** `ROW` matches `| <digits> |` anywhere in the file, so a tabulated discharge joins the absent direction's generous region and exempts every issue it names. Stated in the template body, where the person about to draw that table is looking, and deliberately without naming `check-backlog-tracker.py` — that guard is not shipped, and a shipped document asserting a path a project does not have is #188, filed the day before.
- **The prune is two groomings' worth, not "once the archive has absorbed it".** Rules out a prune conditioned on a sweep nobody is obliged to run.
- **#74's second bullet closes here; the other two stay open.** Rules out #164 carrying its AC6, which becomes a no-op on rebase. Rules in a slightly wider diff on row 1 to avoid leaving two broken rules in a list this spec was already editing.
- **No twenty-sixth entry in `check-skill-contracts.py`.** The cap requires an argument that review cannot defend the sentence, and it cannot be made: the candidate sits inside a template fence a reviewer reads as a unit. The load is carried by the remedy message instead, which a case asserts.
- **Minor, not patch.** Two new template sections change what a grooming produces. The issue's footer said patch and was written before the scope included the remedy.
- **Three review rounds, then the cap.** Eleven findings fixed across rounds 1 and 2; round 3 returned three MEDIUM and four LOW with its own verdict that two were worth the edit. Both sit outside `- Source globs:`, so fixing them did not stale the receipt and no fourth round was owed.

**Accepted, not fixed** — round 3, five findings, recorded in the spec's triage with reasons:

- **`## What changed at this grooming` is unpinned.** It is outside both citation regions by the same construction as its sibling, and no case says so. A sixth fixture closes it in about six lines — and buys a fourth round, for coverage whose marginal value is one heading name. The Design claim was narrowed to what is true rather than left overstating.
- **The AC3 amendment shares a commit with two files AC3 names** (C-8). Substantively defensible — created by round 1's own finding, dated, lowers no bar — but the shape is what C-8 is for. Not split, because rewriting history to satisfy an ordering check is worse than recording it.
- **No placeholder line under `## What changed at this grooming`**, where every other template section has one. One line, shipped path, deferred for the receipt reason.
- **The migrated section sits between two grooming appendices**, because in this document `## Unshaped` is above the table. No guard consequence — verified. The cause is the ordered table having no heading of its own, which this spec put out of scope.
- **`.work_logs/2026-09-21.md:14` records the eighth grooming as `47 open issue(s) against 24 row(s), no drift`**, which cannot be true if it post-dates #188's filing. Found by the reviewer outside the diff. Not corrected here, because this log is append-only and the earlier entry is a record of what was run — the correction is this line.

### Next steps

- [ ] **Open #174's pull request**, closing the issue. Branch `174-a-discharged-row-has-no-home` at 0.18.0, CLEAN receipt at `e32434e`, thirteen validators and 145 cases green.
- [ ] **Place #188, or `check-backlog-tracker.py` keeps this PR red.** Filed 2026-09-20T16:05Z, missed by the eighth grooming, so `main` fails the guard today independently of this branch. It is a bug found on a real consumer install at 0.17.0 — a row or an `## Open, not planned` entry is a product judgment, raised here rather than taken.
- [ ] **The eighth grooming's count needs re-running.** See the accepted finding above; whoever places #188 should re-run the guard rather than trust the recorded line.
- [ ] **`.specs/` holds three live specs** — #79, #182, #174, all `done`. An `archive` sweep is owed, on request, and is not part of any flow.
