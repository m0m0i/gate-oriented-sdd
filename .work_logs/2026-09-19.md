## 2026-09-19 — m0m0i

### Summary

`init` recorded nothing about which document set the operator chose, so the bootstrap block named three documents to someone who may have signed up for six (#141). Ten commits, **0.9.0 → 0.10.0**, test-gates 82 → 87. Five review rounds, and the last two found defects the previous round's fixes had introduced.

### Activities

- Read the four issues filed against a fresh install (#139–#142) before starting anything. Three of the four argue for scope this harness has a rule against: #139's ten-section North Star contradicts `northstar`'s own "One page" and duplicates `design-doc`, `prd` and `epics`; #140's subdirectory layout contradicts `docs/layout.md`'s "the repo root IS the plugin" while its `.gitattributes` option does nothing for a `git clone` install; #142 depends on both and inherits #139's problems. **#141 was the one with a real gap under it**, and its proposed fix was not the fix.
- `141-init-asks-for-the-target-document-set` (#141). `clarify` moved the question out of step 2 — the issue asked for a sixth interview question against a stated five-question budget, and the choice was already sitting unrecorded in step 3.
- `- Target:` is a fourth machine-read line, read **only** while `- Mode:` is `bootstrap`. It changes the two bootstrap messages and nothing else; `wanted` is still built from `- Mode:` alone.
- Guards: an unrecognised target fails loudly (absent does not), the anchor table covers the new key, and two `CONTRACTS` entries pin the question **and** its destination — AC7 is a conjunction and the first round pinned only half of it.

### Decisions

- **The target sharpens the message; it never changes what is required.** A target on the pass/fail path would be a second mode, and a mistyped one would then decide which documents go unchecked. That is the fail-open this repo owns the absence of, so the target is structurally off that path and the reviewer verified it in the code rather than from the spec, in all five rounds.
- **Absent is supported, present-but-wrong is loud.** Every project installed before this line existed has no target, and blocking one would be a failure that predates the user. A typo is different in kind: the bootstrap window is the only stretch anything reads the line in, so swallowing it hides the mistake permanently.
- **The `bootstrap` re-run widening was declined.** A project at `bootstrap` carries a `- Mode:` line, so AC6 and the pinned Rules bullet make it an upgrade that changes nothing else. Taking the widening would mean amending an approved AC; left narrower and worth its own issue.
- **0.10.0, not the 0.9.1 chosen at clarify.** C4 pre-agreed the re-check if the diff landed larger. It did — #110's exact shape, and the work log sized #110 minor for that reason.

### What the review rounds actually cost, and bought

Rounds 1 and 2 found defects in the work. **Rounds 3 and 4 found defects introduced by the previous round's fixes**: a scoping clause that was vacuously false because step 3 writes `- Mode:` before the condition is read, and a docstring exhibit asserting the opposite of the lesson it existed to teach. Neither is reachable by a pin — both pins check that a sentence is *present*, not that it is true or that it ever fires. That is precisely the exposure `observations.md` records for AC1, AC2 and AC6, demonstrated twice on this branch rather than argued.

### Next steps

- #126 is what would move AC1, AC2 and AC6 from pinned to observed: an `init` run against a scratch project. Nothing here claims them as verified.
- Worth filing: the `- Mode:` contract entry has the same unpinned-destination gap this spec closed for `- Target:`; `docs/DESIGN.md`, `CONTRIBUTING.md` and `scripts/test-gates.sh:485` all still say "five machine-read lines" against a guard that now checks seven; and `Target`/`Mode` are likelier prose nouns than the other anchors, so a plain `Target: Python 3.11` in a project's tech.md would false-block.
- Reply still owed on #139, #140 and #142 — the narrowing arguments are in this session, not on the issues.
