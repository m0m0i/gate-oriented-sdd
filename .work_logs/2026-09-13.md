## 2026-09-13 — m0m0i

### Summary

The README's `## Status` section said three false things at once (#115). Nine commits, **no version
bump** — `scripts/` is not shipped — test-gates 76 → 79. Four review rounds, and every finding was
the same mistake at a different distance.

### Activities

- `115-the-status-section-at-0-7-0` (#115). Three claims, three different drift mechanisms:
  `v0.4.3` against a manifest reading **0.7.0**; a behaviour count against a suite reporting 78;
  and "all but two" receipts against **three** that say `inline`.
- **The version is the one worth knowing about.** It was written by #47 on 2026-09-05 and survived
  three releases **and an edit to the same file** (`3eb2299`, for #110) — nothing ties the README's
  version to the manifest bump, and `check-manifests.py` verifies the two manifests against each
  other while knowing nothing about the README.
- `scripts/check-readme-claims.py` — new. Version against `plugin.json`, receipt counts against the
  receipts on disk, both languages, and it fails if a behaviour count reappears.
- Filed **#133** (the prevention half of the version drift) and commented the scope widening on
  #115 **before** writing the spec.

### Decisions

- **Guard two claims, remove the third.** The version and the receipt counts have exact, cheap
  sources. The behaviour count's only source is running `test-gates.sh`, already the slowest
  validator — guarding it would double it every turn to check one integer. **One claim was made
  checkable and the other was made unnecessary**, and the second is the stronger fix wherever it is
  available.
- **Scope widened out loud.** #115's title covers one of the three drifts. The widening was
  recorded as a comment on the issue before the spec existed, with #48 and #97 as the precedent for
  treating `## Status` as one subject. An issue whose scope grows silently is what `sprint` exists
  to prevent.
- **Two findings filed rather than folded in**, on AC8's boundary: #133 would touch `skills/`, a
  shipped path, and owe a bump this spec says it does not carry; #131 is about the reviewer's own
  allow-list and landing it here would break one-issue-one-branch.
- **Two known gaps recorded rather than closed** in the guard's docstring — a trailing bare counter
  in Japanese, and English `cases` — each with the reason the obvious fix is worse. *An unrecorded
  known gap is how round 1's defect survived; a recorded one is a decision.*
- **What ships, stated precisely**: the version claim can no longer go stale **quietly**. It can
  still go stale — #133 owns prevention.

### The four rounds, and the one mistake under all of them

**Every finding was a check narrowed to the example that prompted it — and the example is always
the case that is already fixed.**

- **Round 1** — I scoped the fix to `## Status` because that is where the issue pointed. The same
  count was false **a section above**, in both languages, and the guard I wrote certified its
  absence over the whole file. *A scope drawn from where a defect was reported is not a scope drawn
  around where that defect lives.*
- **Round 2** — removing the count, I replaced it with "every path across the gates and guards".
  `67 paths` was false but **falsifiable**; `every path` is unfalsifiable and unsourced, and G-4's
  own case-7 history is this repo's evidence that coverage is not exhaustive. And the Japanese half
  of the pattern required the noun **flush against the digits**, which is the least common way
  Japanese writes a count — the one form it caught was the one that had shipped.
- **Round 3** — the no-op detector in a fixture used `str.replace(x, x)`, the identity function,
  then concatenated: it could never fire. The *shape* of a guard with the substance removed.
- **Round 4** — clean.

### Three misses I found myself, recorded because they were never seen

A first widening that fired **eight times on a correct file** — URLs, the token count, a table row —
caught before shipping. **Three fixtures whose Python string literals were split by literal
newlines**, dying on `SyntaxError` and no-op'ing, against #124's rule written down the day before.
And a probe that reported nothing because I quoted the needle wrong, found by asking whether the
mutation had applied.

### Next steps

- [ ] Open the PR for #115, closing the issue. Say in it that detection is closed and prevention is
      not, or a reader of the merge commit will conclude otherwise.
- [ ] **#127** remains the most urgent open defect — first-install, gate-armed-red, and #130 says
      fixing it alone is not enough.
- [ ] **#131** — now three validators the reviewer cannot run, and it degrades every future review's
      AC7 coverage.
- [ ] **#133** — the version-drift prevention half.
- [ ] **#124** now has four forms of "a check that could not fire" to record.
- [ ] A real-project `init` run stays owed (#126 used a synthetic target).
- [ ] Branch protection on the `guard` job — carried from 2026-09-12, still untouched.
- [ ] `.specs/` holds six shipped specs live; a sweep is available.

## 2026-09-13 (second session) — m0m0i

### Summary

#127: `init` armed the document-set gate red on every first install. Fourteen commits, **0.8.0**,
test-gates 79 → 81 with a new skip mechanism. Eight review rounds — the code was right after two
of them, and the other six were about claims.

### Activities

- `127-init-arms-the-document-set-gate-red` (#127). `init` step 3 wrote `- Mode:`, copied
  `check-document-set.py` onto the project's `- Validators:` line, and created **none** of the
  three documents that checker requires. CAP-4's falsifier stated literally, and **dormant**:
  `quality-gate.sh` runs that line only when a `- Source globs:` path changed, and a document is
  never source, so the install looked clean and fired on the user's first real edit.
- **Root cause was a seam, not a missing `mkdir`.** The checker asked two questions through one
  exit code — *is the harness installed correctly* (which `init` can make true) and *has this
  project authored its documents* (which it cannot, without writing the user's thinking for them).
- `- Mode: bootstrap` added: claims the install-time invariant only, says so in its own success
  line, and **expires at the first spec**.
- Two fail-open defects found in review, both in the new code: `Path.is_file()` swallowing
  `OSError` one level inside a function whose docstring forbade exactly that, and
  `follow_symlinks=False` producing a *fourth* outcome in a function claiming three.
- `note_skip` in `test-gates.sh`: thirteen self-disabling guards used to report `ok`, two of them
  for whole cases. Applied to all thirteen, including the four that predate this spec.

### Decisions

- **A third declared state, not scaffolded documents and not a tolerant checker.** Running
  `prd`/`design-doc`/`backlog` inside `init` turns install into a multi-hour interview, against
  `init`'s own "treat a greenfield repo as the special case"; placeholders are the `TODO` step 3
  forbids one directory over; leaving the checker off `- Validators:` is the gate switched off at
  install and never switched back on. **Declared, not inferred** — for the same reason `minimum`
  is.
- **The grace period ends at the first spec**, not at the first merged one. A PRD starts being
  cited the moment a spec exists, so the block lands *inside* the flow with the cause one command
  behind the user — which is what CAP-4 actually asks for. It is not "never block"; it is never
  block on a failure that predates them.
- **0.8.0, minor**, on #110's precedent (a value on a machine-read line, and `init` writes
  something different). Rejected 1.0.0: #82 shipped, so the relocation holding the major is done,
  but this changes the installed interface in the same breath as declaring it frozen.
- **AC1 narrowed at round 4**, from *every validator `init` arms* to the one the fixture runs.
  `check-steering-anchors.sh` is armed by step 3 and neither installed nor run by `init_tree`.
  Stated in Design as **armed but unexercised** rather than closed — it is this issue's shape one
  axis over, and worth its own issue.
- **A correction to yesterday's habit, and to a commit on this branch.** `c6a30d9`'s message
  described four fixes; its diff carried two, because the edit script's single write came after a
  raise. That is #126's defect — *a commit that claimed two fixes and made neither* — and it
  happened again here. The commit stands unamended: rewriting a post-mortem would lose the record.
  `f50bf45`'s own message then overstated the mechanism ("both discarded" when one of three
  landed), which the reviewer caught and which this entry corrects.
- **Six of eight rounds were about claims, not code** — a count, a derivation, a commit message,
  four line pointers, and a sha I wrote from memory rather than from `git log`. Recorded in
  `observations.md` because the lesson is not "count more carefully": **a number, a pointer or a
  sha is a claim, and a claim needs the same derivation a gate does.**

### Next steps

- [ ] Merge #127's PR, then **#130** — the second independent cause of the same red gate, on the
      template axis. Fixing #127 alone does not turn step 4's first proof green on a project that
      already had `bug_report.md`; this branch neither fixes nor masks it further.
- [ ] **#131 widened here.** Three of the twelve validators are outside the reviewer's allow-list
      in every round, and one of them is now `check-document-set.py` — the file this branch
      changes. Its logic is covered by fixtures; it was never run against this tree by the
      reviewer. I ran all twelve by hand before each commit.
- [ ] **Branch protection on the `guard` job** — raised 2026-09-12, still untouched, and now
      load-bearing for the three validators above.
- [ ] `bootstrap` has no dogfooding here: this repo is `- Mode: full`, so the new value lives only
      in fixtures — the gap #110 recorded for `minimum`, one value over.

## 2026-09-13 (third session) — m0m0i

### Summary

#130: `init`'s merge rule and the document-set checker disagreed about a template's filename, so
following step 3 correctly produced a red gate. Eight commits, **0.9.0**, test-gates 82. Four
review rounds, no BLOCKER and no HIGH — and every MEDIUM after round 1 was damage the fix did to
the bullet it was fixing.

### Activities

- `130-init-and-the-checker-disagree-on-a-filename` (#130). The merge bullet said *"add only the
  missing types"*; `check-document-set.py` wants a literal `bug.md`. A project with
  `bug_report.md` got neither, and #127 had been masking it — with #127 shipped, this became the
  **first** thing such a project hits.
- **`init`'s side gives.** It now `git mv`s an existing template to the canonical filename, keeping
  body, front matter and labels, with the diff shown and agreed first. `TEMPLATES` is untouched: a
  literal filename is the cheapest correct guard, and it only misfired because the installer was
  told to produce something else.
- The checker's one change is its **failure message** — a project in this state believes it has a
  bug template, so naming only the absent path reads as a false accusation, and a guard that looks
  wrong gets switched off.
- Eighteenth skill-contract pin, widened at round 2 to span the **order** as well as the rename,
  and mutation-tested: swapping the order back with the rename intact reddens the suite.

### Decisions

- **Absorb into the canonical name, not a declared `- Issue types:` line.** The latter is #128's,
  and taking it here would make this spec the larger of the two and close neither cleanly.
  Inferring the type from front matter was rejected on the repo's own reasoning: a project's
  taxonomy is **declared**, not guessed — the same argument that gave #110 a declared mode.
- **Filename axis only.** #128 keeps label→type. The rename dissolves one of its two halves; it
  does not obviate the issue.
- **0.9.0, minor** — `init` gains a behaviour it did not have (it renames a file in the user's
  repository), rather than a better description of one.
- **The Clarification that overstated the licence is left unrewritten.** It claimed step 3's rules
  "already say" a rename needs a diff; they named three paths and their verb was *overwrite*. The
  reviewer was right. The safeguard now lives inline and the Rules line names
  `.github/ISSUE_TEMPLATE/` and says *rename or move* — but the record of what was argued at the
  time stays as it was argued.
- **T3 should not have carried the version bump**, and `.steering/tech.md:21` already said so.
  Two consecutive specs broke it. Annotated rather than rewritten; filed as **#136**, framed as
  *either* guard it *or* revisit #113, because C-6 pulls the other way and a check added to an
  unreconciled rule certifies a decision nobody made.
- **The reorder is the lesson.** Moving a sentence is the cheapest-looking edit in prose, and it
  silently re-scoped "adapt labels" onto a clause that contradicts it. Nothing mechanical saw it;
  nothing mechanical can. That is the honest limit of what a presence pin buys.

### Next steps

- [ ] Merge #130's PR. With it and #127 in, `init` step 4's first proof should be green on both the
      greenfield and the migration path — **untested against a real project**, which is the gap
      #126 left and this spec does not close.
- [ ] **#137** (new) — `check-readme-claims.py` guards "no behaviour count in prose" across two
      files; the claim lives in `.steering/`, `agents/` and the skills too. Found because
      `.steering/structure.md` said *67 paths* against a suite printing 82.
- [ ] **#136** (new) — the version bump as a task, unguarded and now twice-broken.
- [ ] **#131 is now two specs old** on the same file: `check-document-set.py` has been changed
      twice by a branch whose reviewer could not run it.
- [ ] **#128** — the label axis, smaller now than it was this morning.
- [ ] **Branch protection on the `guard` job** — still open from 2026-09-12.
