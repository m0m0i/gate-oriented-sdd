## 2026-09-12 — m0m0i

### Summary

The minimum/full choice `init` had been making since the beginning is now a value the harness reads
and a checker verifies (#110). Ten commits, 0.6.0, test-gates 67 → 72. **The major was not taken**,
and the reason it was not is the more useful half of the day.

### Activities

- `110-mode-the-harness-can-read` (#110). `- Mode: minimum|full` joins the machine-read family in
  `.steering/tech.md`; `assets/check-document-set.py` verifies the filesystem against it and is
  copied by `init` onto the project's `- Validators:` line. Declared rather than derived, because
  derivation cannot tell a deliberate omission from an abandoned install — which is the entire
  feature. Four tasks, each red first: the checker, `init`'s contract, `spec`/`contract`'s wording,
  then both READMEs and this repo's own `- Mode: full`.
- **Three corrections carried in before they were encoded.** The issue's document counts (6 minimum,
  9 full) repeated the double-count #109 had just removed — it is 5 and 8, plus three issue
  templates either way. Its test-gates figure of 62 was stale since #113. And its "gains no case"
  assertion could not survive the checker landing here, since guards are tested in that suite; AC11
  was amended at `clarify`, ahead of any Design, to pin an untouched `hooks/` directly.
- **Reviewer, by subagent, four rounds with its context kept.** BLOCKED ×3 → CLEAN. The artifact was
  right in round 1; every finding after that was about wiring and evidence, and **each round's HIGH
  was introduced by the previous round's fix**. Detail in the spec's `observations.md`.
- Filed **#122** — `check-steering-anchors.sh` counts a whitespace-only value as resolved, for all
  six anchors. Found empirically while reviewing this branch: with `- Docs: <tab>` written, it
  printed `6 of 6 anchor(s) resolved` about a line pointing nowhere.

### Decisions

- **0.6.0, and the major stays unspent — but not for the reason first given.** AC13 was drafted at
  `1.0.0` on the recorded reservation of the major for the minimum/full switch, with the counter-case
  argued from `.steering/product.md`'s "not a supported product". **That argument was wrong and is
  withdrawn.** This is an open-source repository, so nobody or everybody supports it, and that line
  disclaims *support* — an SLA, a maintainer on call — which is true at every version and says
  nothing about compatibility, which is what a major actually signals. Two senses of one word,
  collapsed. The real counter-case is **#82**: it must relocate the reviewer contract to one settled
  path, three documents currently place it three ways, and landing that after `1.0.0` breaks every
  consumer pinned to the old location — a `2.0.0` by semver. This plugin has downstream consumers
  that pin copied modules per version, so that is migration work someone actually performs rather
  than a hypothetical. So the reservation is honoured rather than overridden: **the trigger has
  arrived, the interface is not yet safe to freeze, and `1.0.0` follows #82.**
- **The checker is a guard the project owns, never a hook.** `quality-gate.sh` runs whatever
  `- Validators:` names and knows nothing about modes. A gate that branched on mode would be a
  switch that turns enforcement down, which is AC5 — and declining to close the turn-end gap is that
  criterion working, not a hole left open.
- **CI is where this one actually bites, and that was a HIGH.** The `- Validators:` line runs only
  when a path matching `- Source globs:` changed, and a document is never source: the two sets are
  **disjoint by construction**, because one line points at documents and the other at code. So the
  checker ran on exactly the turns where nothing it checks could have changed. Demonstrated before
  fixing — `rm docs/CONTRACT.md`, checker exits 1, turn-end gate exits 0. Fixed with a CI step and
  the sentence `init` already carried for `check-locks.py`.
- **`contract` stays out of `minimum` and runnable at any time**, because its own step 1 asks for the
  last fifty commits and its red flags name copying a style guide wholesale — run early it produces a
  *worse* rulebook rather than an absent one. And `spec`'s side-door accusation is scoped to `full`,
  since in a project that never batches `sprint` it fires on every spec and trains the user to skip
  the rule that matters.
- **Accepted rather than fixed:** round 4's LOW — `cf1`/`cf2` are absent from the case's detail
  string, so a red run does not say which half broke. `scripts/` is inside `Source globs`, so fixing
  it would re-stale a CLEAN receipt and buy a fifth round for one line of diagnostics. The remedy is
  written down for whoever next touches that file.
- **One receipt field deviates from verbatim, and it is recorded rather than quiet.** The reviewer
  wrote `high=1` and flagged the ambiguity itself: rounds 1-3 each carried a HIGH against an earlier
  SHA, and none remains at `ae1d607`. Written `high=0`, because the field describes the reviewed SHA
  and #109's own final receipt is the precedent.

### Three lessons, all earned the expensive way

- **A fix is not finished when it is correct; it is finished when reverting it turns something red.**
  Round 2's parity fix shipped unpinned, and restoring the defect left all 71 cases green.
- **A safety check is not a safety check until something fails when you break it.** Fixing one
  silent-green fixture took three attempts: `assert` (rejected by this suite's own guard — `python -O`
  strips them, #28), then a bare `raise` (the shell does not inspect a heredoc python's exit status,
  so it died into stderr and the case passed anyway), then binding the status into the report.
- **Mutate the fixture, not only the subject.** The second failure above was found by breaking
  `docset_repo`'s format string and watching the case stay green. Every mutation test here until now
  has broken the thing under test; this one broke the thing doing the testing, and nothing in the
  rulebook tells anyone to try it.

### Next steps

- [ ] Open the PR for #110 from `110-mode-the-harness-can-read`, closing the issue.
- [ ] **#82 is now the gate on `1.0.0`** — it settles where the reviewer contract lives, and the
      installed layout cannot be frozen until it does. That reframes it from "one of four open `init`
      bugs" to the critical path for the major.
- [ ] **Confirm branch protection requires the `guard` CI job.** That job is now the only thing
      between a deleted document and a green branch, since the turn-end gate structurally cannot see
      `docs/`. Raised by the reviewer; not visible from a session.
- [ ] Consider rewording `.steering/product.md`'s "Not a supported product" — in an open-source repo
      it reads as a compatibility claim it does not make, which is exactly the confusion that
      mis-argued AC13 today. Its own issue, not this branch.
- [ ] Still open: **#112 and #111** are superseded by #119 and #118 and are double-counted in the
      backlog. Carried from 2026-09-11 and untouched.
- [ ] `.specs/` now holds three shipped specs live (#113, #109, #110) — a sweep is available.

## 2026-09-12 — m0m0i (second session)

### Summary

`#82` — one path for the reviewer contract, the issue holding `1.0.0`. Twelve commits, 0.7.0,
test-gates 72 → 76. **The major was still not taken, and this time the reason is evidence rather
than judgment.**

### Activities

- `82-one-path-for-the-reviewer-contract` (#82). A shipped reviewer's first instruction named
  `agents/_shared/reviewer-contract.md` — the *plugin's* directory, absent from every install —
  and the contract answers a reviewer that cannot find it with "say so and stop". So a consumer
  copying a reference reviewer got one that declines to review: CAP-1 falsified at install,
  silently, because it is indistinguishable from a careful reviewer being careful.
- **Two corrections to the issue, verified first.** No lock re-pin is needed — the locks hash
  `rules/*.md`, six pinned files, none of them a reviewer. And its proposed fix,
  `.claude/agents/_shared/…`, hardcodes one harness into files that ship to two.
- **`scripts/check-contract-path.py`** is new: ten sources, two rule groups, six shape clauses
  that run before any comparison. On the `- Validators:` line and in CI.
- **Reviewer, by subagent, six rounds:** BLOCKED ×4 → CLEAN → APPROVE. Four HIGH, none in the
  feature.
- Filed **#124** — a `G-*` clause for the failure this branch hit four times.

### Decisions

- **0.7.0, and `1.0.0` now waits on a run rather than on work.** The reviewer's verdict, held
  across four rounds and relayed as its own: this branch was wrong **twice** about where a
  reviewer can open its contract, and both times the error was invisible to eleven green
  validators and obvious within seconds to something that *tried the instruction*. Six rounds
  made the guards trustworthy; the interface being settled is a different claim. `1.0.0` when an
  `init` run on a scratch clone shows a shipped reviewer opening its contract with no hand-edit,
  as a `docs/verified.md` row — a run already owed for #81, #83 and #84.
- **The issue's fix was overruled**, and the reviewer-relative form it was replaced with **was
  also wrong**. A reviewer reads its own instruction with the **project root** as its working
  directory, so a bare relative path resolves from neither root. Reviewers now name the relative
  form *and* both concrete destinations; a shipped template must carry **both**, an install its
  own.
- **The grouping is checked, not just documented.** `SHIPPED` must live under `agents/`,
  `INSTALLED` under a harness directory, `SUFFIX` may hold neither — because moving one entry
  between tuples grants it the weaker obligation in a diff that reads as tidying, and the
  `SUFFIX` rule is one under which the original defect passes.
- **Accepted rather than fixed:** nothing. Every finding across six rounds was fixed, including
  four advisory ones, because each was a fail-open or the pattern that produces them.

### The lesson this session actually taught

**An independent reviewer that *executes* finds what one that *inspects* cannot.** Round 1's
second HIGH exists because the reviewer followed the new instruction, failed, and guessed the
wrong file. The fix was internally consistent, every validator was green, and it was wrong.

Beside it, the failure this branch repeated four times — **a check that could not fire**: an
assertion reading the wrong stream; a fixture whose regex missed a one-line tuple and crashed the
guard instead of mutating it; a `\n` that reached a heredoc as a literal newline; a clause pinned
by nothing. Each invisible to a green suite, each found only by breaking it on purpose and
**reading why it went red**. That is #124.

And twice the repair carried its own defect in: round 1's left `init` instructing operators to
delete exactly what it had added; round 2's `any` let a shipped reviewer serve one harness.

### Next steps

- [ ] Open the PR for #82 from `82-one-path-for-the-reviewer-contract`, closing the issue.
- [ ] **The `init`-on-a-clone run is now the critical path to `1.0.0`**, and it closes #81, #83
      and #84 at the same time. One extra thing to observe: which of the two labelled paths a
      reviewer picks under Antigravity.
- [ ] **#124** — the `G-*` clause. A rulebook change, its own spec.
- [ ] **Confirm branch protection requires the `guard` job.** Carried from this morning, untouched.
- [ ] `.specs/` now holds four shipped specs live (#113, #109, #110, #82) — a sweep is available.

## 2026-09-12 — m0m0i (third session)

### Summary

Ran `init` at 0.7.0 against a scratch project (#126) — the verification `1.0.0` was waiting on.
Eleven commits, **no version bump**, test-gates unchanged at 76. **Three new defects from the run,
two more from its review, and #82's fix confirmed working on a real install.**

### Activities

- `126-run-init-on-a-scratch-project` (#126). Filed as **acknowledged unplanned work** rather than
  created silently — `spec` step 1 refuses to start without an issue, and that refusal only means
  something if creating one is said out loud. A `chore`, so the controlling line is what must not
  change: this repository's gates, `init` itself (a defect it exhibits gets an issue, not a fix),
  and the reviewer's independence.
- A synthetic TypeScript project with runnable validators, an existing issue template carrying its
  own labels, and a commit convention that is not the harness's. Baseline captured **before**
  anything ran; 11/11 validators identical afterwards; the project deleted in T4.
- **Reviewer, by subagent, seven rounds.** BLOCKED never — CLEAN throughout, with findings at every
  round until the last.

### What the run found

- **#127** — `init` installs `check-document-set.py` and **never creates the documents it
  requires**. Step 3 opens with "scaffold the mandatory set" and not one of its ten bullets writes
  `PRD.md`, `DESIGN.md` or `BACKLOG.md`. The gate is armed **red by the harness's own file**, and it
  is **dormant** until the user's first source edit, because `quality-gate.sh` only runs the
  `- Validators:` line when a source glob moved. That is CAP-4's falsifier stated literally, and it
  arrived with #110 — shipped this morning.
- **#128** — the template merge leaves `spec` with no way to read an issue's type.
- **#129** — step 2 asks the operator to confirm a branch convention `review-gate.sh` will reject.
- **#130**, from the review — `check-document-set.py` also requires a literal `bug.md` that step 3's
  merge bullet tells the installer not to create, hidden behind #127's fail-fast. **Fixing #127
  alone would not turn proof 1 green** on the merge path.
- **#131**, from the review — the dogfood reviewer's allow-list has drifted from the `- Validators:`
  line, missing both validators added today. It could re-run nine of eleven and recorded the other
  two as *unknown rather than as agreeing*.
- **#83 confirmed. #84 not reproduced** — a clean install has no `CONTRACT.md` for it to fire on,
  which says nothing about whether it is fixed. **#81 not observed** — `ruff` absent. Three
  categories, kept apart on purpose.

### Decisions

- **`1.0.0` is still not cut, and the reasoning is the reviewer's, relayed in full.** Discharged for
  **Claude Code**: a shipped reviewer installed by `init` opens its contract from the project root
  with no hand-edit, via the second of the three paths it names — evidence from a run, which is what
  #82's fix was speculative about. **Explicitly not discharged for Antigravity**: the `.agents/`
  destination has never been opened by anything. And **#127 is a release blocker though not a review
  blocker** — a first-install gate armed red by the harness, discovered one edit later.
- **No version bump**, and it is the first spec in four without one: `docs/` is in neither
  `Source globs` nor `check-version-bump.py`'s `SHIPPED`.
- **A synthetic target tests CAP-4 weakly**, and that is in the `docs/verified.md` row itself rather
  than a footnote. A real-project run is **still owed** and now listed as such.
- **Nothing was accepted-rather-than-fixed** across seven rounds, except one INFO on the reviewer's
  explicit disposal.

### What seven rounds of reviewing a record taught

Rounds 1–2 found defects in what the run recorded. **Rounds 3–7 found defects only in sentences
about the reviews**, each created by the round before it. Three distinct forms of one failure:

- *a qualifier does not travel with the sentence that gets quoted* — the `#82` claim was qualified in
  one place and quoted from another;
- *writing the rule down did not make me apply it* — I recorded that principle and then missed its
  third instance in the same file;
- *text adopted from elsewhere does not inherit the checking that the text around it got* — two wrong
  counts entered this record because the **reviewer** wrote them in a report and I trusted the report
  over the file.

And the sharpest: a commit that **claimed two fixes and made neither**, because a script threw before
`write_text` and I committed the narrative without re-reading the file. #124 was filed for four
instances of "a check that could not fire"; this adds *a report that could not be false*.

### Next steps

- [ ] Open the PR for #126, closing the issue.
- [ ] **#127 is now the most urgent open defect** — first-install, gate-armed-red, and #130 says
      fixing it alone is not enough. It is also the newest, shipped today.
- [ ] **#131** — the allow-list drift, which is why only nine of eleven validators are
      independently confirmable. Sibling of #118.
- [ ] **#124** now has three forms to record, not one.
- [ ] A **real-project** `init` run stays owed; this one does not discharge it.
- [ ] Branch protection on the `guard` job — carried from this morning, still untouched.
- [ ] `.specs/` holds five shipped specs live; a sweep is available.
