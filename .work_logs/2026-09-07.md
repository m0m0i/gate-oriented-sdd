## 2026-09-07 — m0m0i

### Summary

Swept the seventeen shipped specs into `.specs/_archive/`, so `.specs/` holds live work only — nothing, at the moment.

### Activities

- Archive sweep, two pull requests where one was meant: #99 carried the `git mv` renames only, because the status edits were made after the moves were staged and the commit took the index; this branch carries `Status: archived` and the `Archived:` date for all seventeen, in the format the earlier archives use. Two specs keep their status on the slug line rather than a fixed line number, which the first pass missed.

### Decisions

- **Sweep on request, not per merge**, as the skill says: seventeen finished specs against eight archived was the noise threshold.
- **Archived date on the Author line**, matching the eight already there, rather than a new field.
- The second pass flipped eight of the seventeen: the script stopped at a spec whose body mentions `Status: done` in prose, and the chain did not stop with it. This branch flips the remaining nine by matching the slug line only, and checks all twenty-five before committing.

## 2026-09-07 — m0m0i (second entry: #102's branch)

### Summary

"Unverified" is the word wherever the repository still said the eval suite has never run or the Antigravity rows were not re-run (#102). Ten files; the same fact in the word the north star's third law already uses.

### Activities

- `102-unverified-is-the-word` (#102): five commits. The north star's quote follows the heading it quotes; `verified.md`'s preamble agrees with its table; two dated observation rows keep the old phrasing as history under an AC1 amendment that landed ahead of the artifact — the T2 commit was undone and re-made behind it so the tick reads as an independent check.
- Reviewer, by subagent: CLEAN on the first pass, two LOWs applied to the record.

### Decisions

- **The author's wording, and the reason it holds:** "have never executed" and "unverified" state the same fact, and the second is a label rather than an apology. The non-negotiable — no check reported as having run when it did not — is unchanged in letter and meaning.
- **Records stay records.** `.specs/`, `.work_logs/` and `verified.md`'s dated rows keep what they observed.

## 2026-09-07 — m0m0i (third entry: #104's branch)

### Summary

Both READMEs count `archive` as five sweeps now, and the Japanese README's remaining translated sentences read as Japanese (#104). Fifteen sentences outside the Status section, each carrying the claim its English makes; two that had drifted from the English brought back.

### Activities

- `104-ja-natural-and-five-sweeps` (#104): six commits, two READMEs. Reviewer, by subagent, reading both languages against each other independently of the record: CLEAN with 2 LOW and 4 INFO → the LOWs fixed, three INFOs taken as further Japanese fixes, the fourth filed as #105 → CLEAN with 1 LOW on the record, fixed with the receipt.
- Filed #105: the reviewer contract requires a `reviewed_at` timestamp that no reviewer's allow-list can produce, so every reviewer this week resolved it differently.

### Decisions

- **A targeted pass, not a rewrite.** The Japanese README is the author's own prose; twelve sentences read as translated and were recomposed, the rest untouched.
- **Two claims corrected while recomposing:** the "every project size" sentence the English never made, and 数回 where the English says "perhaps once". Natural Japanese does not license a different claim; C-3 held both times.
- **The reviewer's three older C-3 gaps were taken**, not deferred: the author asked for the Japanese to be right, and each was one phrase.
- **Table rows are padded by display width**, which a character count gets wrong for double-width text; the first fix commit got it wrong and the second got it right.
