# Work log — 2026-09-24

## 2026-09-24 — m0m0i

### Summary

Cut `gate-sdd--v0.21.0`, the first release since v0.10.0 — fourteen versions and thirty commits, with notes reconstructed after the fact. No code changed; this session only read the tree and tagged it.

### Activities

- **Verified before tagging rather than after.** Twelve validators green, `test-gates.sh` 163 passed / 0 failed / 0 skipped, `claude plugin validate . --strict` clean, and `scripts/package-release.py` run locally into the scratchpad to confirm the payload builds outside CI.
- **Tagged with `claude plugin tag`**, annotated `gate-sdd 0.21.0` at `1f55b71`, then pushed `refs/tags/gate-sdd--v0.21.0` and created the release with `gh release create --verify-tag --notes-file`.
- **Release workflow run 35962519352 succeeded** and attached `gate-sdd.zip` at 132,249 bytes. **This is the first release that carries the #140 artifact** — the workflow has existed since 0.14.0 and had never had a tag to fire on.
- **Wrote the notes from the pull request bodies**, in the aggregating shape v0.9.0 used: `Fixed`, three `Added` groups, `What else happened under this range`, `Not verified`. Their subject is six consecutive fail-open defects in this repo's own enforcement layer — #143, #26, #182, #177, #39, #194 — each of which produced output byte-identical to a real pass.
- Confirmed the reconstruction against the tree rather than the pull request titles: PR #183 closes #79 and PR #186 closes #182 via `closingIssuesReferences`, the four archive sweeps total twenty-five specs, and the case count at the v0.10.0 tag was 87.

### Decisions

- **Pushed the tag, then created the release with the notes in the same breath, rather than pushing and editing afterwards.** `.github/workflows/release.yml` fires on the tag and `softprops/action-gh-release` will *create* a release if none exists — so tag-then-edit races the workflow for who writes the notes, and the loser is the hand-written copy. Creating the release first leaves the workflow with an existing release to attach to, which is the path it is written for.
- **`claude plugin tag` rather than `git tag`.** It validates `plugin.json` against the enclosing marketplace entry, which is the check `CONTRIBUTING.md` names for a release. Its nine frontmatter warnings on `agents/**/rules/*.md` and the `CLAUDE.md`-at-plugin-root warning are pre-existing and were not acted on here; a release cut is not the place to change shipped files.
- **Published before the user had read the notes.** The ask was explicit and the session is remote, and `gh release edit --notes-file` makes the notes correctable without disturbing the tag or the asset. Recorded because the alternative — holding a release for a hundred lines of prose to be read on a phone — was the other real option.
- **Kept a `Not verified` section instead of letting `Added` read as proof.** #146 and #147 are pinned by skill contract with no `agy` run behind them, which is what `docs/verified.md:259` already records; an entry claiming otherwise would be the failure that file exists to prevent.
- **This entry gets a pull request of its own, which the skill's own rule forbids in the case it was written for.** That rule scopes to `worklog` run after `implement`, where the entry rides the issue's branch. A release cut has no issue and no spec branch, so there is nothing to ride; PR #159 is the precedent for the standalone form.

### Next steps

- [ ] **A ninth grooming is owed.** `check-backlog-tracker.py` is green at 48 open issues against 21 rows, but eight entries now sit in `## Open, not planned` — #36, #176, #184, #185, #191, #195, #198, #199 — and the eighth grooming (PR #187) saw none of the last five.
- [ ] **Nothing is owed on `.specs/`.** PR #202 swept it to `README.md` and `_archive/` alone, so no archive sweep is pending.

## 2026-09-24 — m0m0i (second session)

### Summary

The ninth grooming of `docs/BACKLOG.md`. One ordering change, one issue rescued from a state the guard cannot see, and five exclusions folded into a row because their conditions formed a cycle.

### Activities

- **Ran `check-backlog-tracker.py` first, then the comparison it is not built to make.** The check was green — 48 open issues against 21 rows, no drift — and the list was still wrong: **#74 has been open since the fourth grooming and cited nowhere but in row 6's reasoning.** The guard's absent direction reads the whole row by design, so a citation in a `Why here` cell satisfies it indefinitely. #74 now sits in row 6's `Item` cell, where #164 closes it outright.
- **Folded #176, #191, #195, #198 and #199 out of `## Open, not planned` into row 2.** Read as a set, their reopening conditions form a cycle — #191 waits on row 1 shipping without it, #198 waits on anyone opening `review-gate.sh` for #176, #178 or #191, #199 waits on #198, #176 waits on whatever touches that `case` next — and every one is satisfied by the row holding #178 and #179, which existed throughout. The section is down to three entries, each with a condition that does not depend on another entry in it.
- **#81 to row 1, the guard sweep to row 2** — the only ordering change. Row 2's `Blocks` is discharged rather than renumbered, and its size re-measured from ~2–3 to four-batched or seven one-for-one.
- **Resolved the three "below 4" references** rows 5, 9 and 12 have carried since before the eighth grooming, all by one fact.
- **Pruned two preamble bullets and the fifth grooming's record.** Net size barely moved — 63,541 → 62,578 bytes, 176 → 185 lines — because this grooming's own record cost about what the pruning recovered. Worth stating plainly rather than claiming a reduction: the pruning stopped the file growing, it did not shrink it.

### Decisions

- **#81 above the guard sweep, reversing the eighth grooming.** That grooming put #39 above #81 on two grounds — unconditional, and unblocks three rows — and #39 shipped as #196 taking both with it. What is left of the row is seven items that each describe themselves as latent: "identical on `main`", "latent today", "durability rather than a defect". #81 reproduces on a first turn and is CAP-4's falsifier. Asked which ships if only one can, the answer is the user. Row 1's own cell had asked the ninth grooming to decide this.
- **#122 and #69 deliberately *not* folded into row 2**, though they are the same class. The seven folded items share two files and a pin; these two are in `assets/`, so a sweep of the gates does not open either, and batching them would buy a larger row and nothing else. Recorded because the fold and the refusal use the same test and it should be visible that the test was applied twice.
- **The #74 finding is recorded as a trigger, not filed as an issue.** `backlog` creates no issues, and one witness does not distinguish an accident from a habit. A second open issue found cited only in reasoning is what makes it a change to `check-backlog-tracker.py`.
- **Pruned rather than carried.** The two preamble bullets both existed to hand an assignment to this grooming, and it took both; a bullet whose assignment is discharged is an instruction nobody needs sitting in the first thing every reader sees. The fifth grooming's record goes to a pointer under the two-most-recent standard `## Discharged since last grooming` sets for itself, and the sixth's one sentence citing its `—` cell count is reconciled in place rather than left dangling.

### Next steps

- [ ] **`## Unshaped` has stood at one item for five groomings**, and its one entry is blocked on row 16, which has not moved either. That is now a finding about the section rather than the item, and this grooming did not act on it.
- [ ] **Rows below 13 have not moved in five groomings.** The last grooming to re-read every row against its issue was the sixth. Either a stable tail or an unexamined one, and no grooming since has been able to say which.
- [ ] **`sprint` is what comes next if any of this is to be built.** Rows 1 and 2 are both shaped and sized.
