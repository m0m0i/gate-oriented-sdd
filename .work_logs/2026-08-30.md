## 2026-08-30 — m0m0i

### Summary

Ran the first half of the inception chain against this repository and recorded what the skills
actually did. Four review rounds; the deliverable is `docs/verified.md`'s new section, not the
three documents it produced.

### Activities

- `55-run-northstar-prd-epics` (#55): all six tasks, T1–T6. Baseline captured and re-verified
  (eight validators, `test-gates.sh` 52/0, `- Owns:` sha, three file checksums); `northstar`,
  `prd`, `epics` run in dependency order; `docs/NORTH_STAR.md`, `docs/PRD.md`, `docs/EPICS.md`
  written; `.steering/product.md` gained a 19-line capability summary with zero deletions;
  `docs/verified.md` gained an 11-row section.
- Reviewer: four rounds. BLOCKED (1 HIGH, 5 MEDIUM) → BLOCKED (1 HIGH, 2 MEDIUM) → CLEAN with
  1 MEDIUM + 4 LOW → CLEAN with 3 LOW. Receipt at `7916a52`, `reviewed_by=subagent`.
- Issues filed from findings: #57, #58, #59.

### What the run found

- **Both citation seams are checked forward only.** `prd` verifies every capability names a real
  lever and never that a lever is served; `epics` verifies every epic names a real capability and
  never that a capability has an epic. First drafts left `LV-2`, then `CAP-3` and `CAP-4`, serving
  nothing — and passed. Two skills, one defect.
- **`northstar` step 6 and `prd` step 7 both write `.steering/product.md`, and neither states a
  merge rule.** The additive result came from the operator. "Summarise into" is equally satisfied
  by replacing the file, which would destroy the `Owns:` anchor — #34's cost arriving through a
  skill instead of a parser.
- **`epics` has no vocabulary for a product that already ships.** Step 6, its red flag, and the
  `Walking skeleton: yes | no` field all assume greenfield, and nothing warns a mature project
  away from a false designation.
- **The `Owns:` collision path was not exercised.** `northstar` derived the value already present,
  so nothing was written. Recorded as unobserved rather than counted as a pass.

### Decisions

- **Documents kept as provisional, not agreed.** `PRD.md` carries `Status: draft` and AC4b
  requires `verified.md` to state that nothing cites the capability ids. Rules out treating the
  run as a product commitment; keeps the artifacts AC4/AC5 are checked against. Rejected:
  discarding them (destroys the evidence) and marking them agreed (a commitment as a side effect).
- **AC8 amended to permit `prd`'s write to `.steering/product.md`.** The scoping error was in
  #55's split from #56, not in the skill. Rejected: skipping step 7, which would have meant not
  running `prd` and left two of three skills with an unexercised seam.
- **AC5 amended to accept a recorded reason in place of a walking-skeleton designation.** Rejected:
  marking an epic `yes`, which fills a precisely defined field with a different meaning.
- **The version matrix in `docs/verified.md` was deliberately not re-dated.** This run exercised
  skills, which are Markdown the harness reads rather than an integration with it, so it produced
  no evidence about the Claude Code or Antigravity versions named there. #48 still needs a run
  that tests them.
- **Propose-then-correct was used for product judgment, and it went quiet.** Four pauses, six
  questions, all six accepted as recommended. Recorded as a limitation in both files: this run
  tests the skills' mechanics and seams, and says nothing about whether they elicit good decisions
  from someone who does not already have the answers.

### Reviewer findings accepted rather than fixed

Three LOWs from the final round, left unfixed so the tree stays byte-identical to the reviewed
sha `7916a52`:

- `observations.md:125` — a 19-column orphan line left by a rewrap. Cosmetic.
- `observations.md:112` — "has run **twice** as of T3" sits four lines from "four pauses carrying
  six questions". The reviewer's own note: nothing in it is untrue; the two counts have different
  scopes and both are correct under their stated rules.
- `spec.md:41` — AC2's continuation at column 0 splits the `- Owns:` string across a line break,
  making it awkward to grep in the spec. The anchor itself in `.steering/product.md` is intact and
  greppable; this is the spec's copy of it.

Fixing them would have moved the tree off the reviewed sha for three cosmetic changes. If any is
worth doing, it rides the next branch that touches these files.

### Next steps

- [ ] Open the PR for #55 and merge it.
- [ ] #56 — `contract` and `design-doc`, which write state the gates already read. The
      merge-rule finding above is the thing to watch: `design-doc` step 5 writes
      `.steering/structure.md`, which is live and parsed by three hooks.
- [ ] #54's spec now has incidents to cite — the forward-only check and the missing merge rule,
      both with this run as evidence.
- [ ] #58 re-grooms `docs/BACKLOG.md`, whose preamble this branch falsified by construction.
