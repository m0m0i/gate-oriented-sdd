# Work log — 2026-09-26

## 2026-09-26 — m0m0i

### Summary

#81 implemented on `81-init-arms-the-gate-red-with-its-own-file`: the two shipped Python guards are clean under a consumer's `ruff`, CI now holds `assets/` to that toolchain in its own `lint` job, and the prose that made the old state look intended is reconciled. Reviewed CLEAN by the spawned reviewer at `ed1b588`; 0.21.0 → 0.21.1.

### Activities

- **T1, `cab406d` — the two files.** Red recorded first with `uvx --from ruff==0.16.9` and `uvx --from shellcheck-py==0.11.0.1`, since neither tool is installed here: the four `ruff check` findings the spec tabulated (`I001`, three `E501`) and two `ruff format` reflows, exactly; `UP017` only at `--target-version` py311+. Green: imports sorted, `datetime` swapped for `time.gmtime`, three lines wrapped, formatter applied. AC5 measured rather than trusted — identical stdout/stderr/exit for both guards before and after; `--update` run with the old and the new script on a deliberately drifted `agents/python-reviewer/` lock in a scratch worktree wrote `generatedAt` in the same 20-byte form both times; `test-gates.sh` 163/0/0 both sides. AC6 clean at py39, py310, py311, py313.
- **T2, `bfd174c` — the `lint` job.** Pinned `ruff==0.16.9` and `shellcheck-py==0.11.0.1` (both confirmed to resolve before they were written in), `--isolated`, no `--target-version`, `actions/setup-python@v7`. Proved with the job's own three commands in detached worktrees: merge-base `38b913f` exits 1/1/0, tip exits 0/0/0. Transcript captured for the pull request body; nothing red was pushed.
- **T3, `ed1b588` — the prose.** Fourth "not on the `- Validators:` line" section in `tech.md`; `CONTRACT.md`'s G-5 cell, `lint` row, formatter paragraph and a new M-16 row; `docs/verified.md`'s record of what is mechanical now and what is not, plus one Still-to-verify row ticked because this branch actually ran it.
- **Review:** `gate-sdd-reviewer` as a subagent, `APPROVE WITH NITS`, CLEAN. All thirteen validators reproduced, `check-backlog-tracker.py` and `claude plugin validate --strict` too. The reviewer could not reproduce AC1/AC2/AC6 (`uvx` is off its allow-list) and said so as a limit rather than a claim.

### Decisions

- **CLEAN is the stop; the three MEDIUM and two LOW are recorded here, not fixed.** Fixing after the receipt re-stales it, and this repository has twice paid for a review round spent on below-HIGH findings after a CLEAN verdict. Each, with its reason:
  - *MEDIUM, `ci.yml` globs are top-level only while the record says "every file under `assets/`".* True, and the trade was deliberate — a glob that matches nothing is an error, a directory holding no Python files is a pass. `assets/` holds no nested `.py` or `.sh` today, so the guard's real scope and its stated scope coincide until someone adds one. The reviewer's `shopt -s globstar failglob` form keeps both properties and is the fix when that day comes; it rides with the `scripts/` follow-up below.
  - *MEDIUM, the AC7 amendment shares `ed1b588` with `docs/CONTRACT.md`, the artifact AC7 is checked against (C-8).* Recorded as the reviewer's alternative asked: the independent evidence is the rulebook's G-5 at the merge-base — `git show 38b913f:.claude/agents/gate-sdd-reviewer/rules/gates-and-guards.md` carries no toolchain note, so the amendment narrowed AC7 to what the repository said, not to what the branch wished.
  - *MEDIUM, `docs/DESIGN.md:13` still counts "the two CI-only" guards.* Stale by one before this branch (`check-backlog-tracker.py`, #79) and by two after it. Pre-existing and outside the spec's affected files; a next step, not a re-review.
  - *LOW, the `ci.yml` shellcheck comment says G-5 covers "the shipped shell"; G-5 says hooks.* The CONTRACT cell is precise about it; the comment is loose. Next step, with the above.
  - *LOW, Clarification 4, the Affected-files bullet and T3 still name the rulebook copy the AC7 note says never existed.* The AC7 note carries the correction; the spec's own convention is to record a reopening rather than smooth it over, and three parentheticals would restate one fact.
- **The two follow-up issues are filed after merge, not on this branch.** `scripts/` under the same configuration (106 findings, 11 files, never shipped) and `hooks/gate-lib.sh`'s four deliberate `SC2086`s (suppress with the reason, never quote). Filing mid-branch reddens `check-backlog-tracker.py` on this pull request, and neither `## Unshaped` nor `## Open, not planned` is shaped for an issue that does not exist yet.
- **`actions/setup-python@v7` beside the existing `checkout@v4`.** A new line starts on the latest major; the v6 note (node 24) and the v7 note (ESM, `pip-install` input removed) touch nothing the job uses. Verified the tag exists before writing it.
- **Rejected `# noqa: UP017` a second time, on the spec's evidence** rather than re-deriving it: with `RUF` selected an unused `noqa` is `RUF100` at py39/py310, so the pragma relocates the defect. `time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime())` leaves nothing to flag at any target version.
- **Ticked one Still-to-verify row in `docs/verified.md`** — `--update` on a lock with a changed rulebook — because AC5 ran exactly that path, on a shipped lock, in a scratch worktree, and it re-pinned. A row ticked from a run is what that file is for; it had been open since August.
- **`Status: done` lands after this entry**, in the order asked for: bump, work log, flip. The gate reads the receipt, not the Status line, so the order is a convention rather than a mechanism.

### Next steps

- [ ] **After merge, file the two follow-ups the spec names**: lint `scripts/` under the same configuration, folding in the recursive-glob MEDIUM and the G-5 comment LOW; and suppress `hooks/gate-lib.sh`'s four `SC2086`s with their reason so `hooks/` can be linted without being "fixed".
- [ ] **`docs/DESIGN.md:13`** — drop the count of CI-only guards or point it at `tech.md`'s four exclusion sections.
- [ ] **When the pull request's `lint` job has its first green run, check it is in `main`'s required checks.** A red job nobody must wait for is advisory, which is the shape this project refuses; the reviewer flagged it as INFO and it is decided on GitHub, not in the tree.
- [ ] **A tenth grooming**: #81 leaves row 1 and row 2 takes it on the evidence the ninth grooming already wrote down.
