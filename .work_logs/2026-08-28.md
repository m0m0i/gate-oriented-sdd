# 2026-08-28

## 2026-08-28 — m0m0i (Claude Code)

### Summary
`G-9` added to the reviewer's own rulebook (#45), after four review passes in which the rule written to prevent a fail-open twice contained one. Two new issues filed from what the work turned up.

### Correction to the 2026-08-26 entry
That entry's next-steps records the reason for deferring `G-9` off #10's branch as *"that rulebook is hash-pinned by `check-locks.py`"*, pointing at `agents/*/rules/gates-and-guards.md`. **Both halves are false and the entry stays as written**, per this skill's rule that a corrected past is not an audit trail:

- No `gates-and-guards.md` exists under `agents/`. The G-series lives only at `.claude/agents/gate-sdd-reviewer/rules/`; the three shipped reviewers carry `COR-*`/`TST-*` and language rulebooks with no guard rules at all.
- That file is **deliberately unpinned** — `.claude/agents/gate-sdd-reviewer/` has no `rules-lock.json`, and `AGENTS.md`:74-77 explains that adding one reintroduces #16. So no re-pin was ever required.

Separating the work was still right; the reason recorded for it was not.

### Activities
- `45-g9-normalisation-asymmetry` (#45): spec → clarify → implement → four reviewer passes → CLEAN.
  - **T1 audit** of every guard in `scripts/`, `assets/`, `hooks/`. One live instance, already fixed and pinned by #10. Its real finding was a normalisation on the *accusing* side that is nonetheless safe — `check-templates.py`'s `line.strip()` — which forced the rule to be precise rather than sweeping.
  - **T2** wrote `G-9`; **T3** confirmed `check-locks.py` still reports six pinned files with no lock under `.claude/agents/`.
- Filed **#48** — both READMEs claim `v0.2.3` in their Status section against a shipped 0.3.9, in the sentence that says this harness exists to prevent unverified assertions. `check-version-bump.py` looks like it would catch that and explicitly exempts the READMEs at :18.
- Filed **#49** — `gate-sdd-reviewer`'s Bash allow-list names six validators against the Validators line's eight, so it cannot run `check-templates.py` or `check-steering-anchors.sh`. #14's shape in a different pair of documents.

### Decisions
- **`G-9` lands only in the dogfood reviewer's rulebook.** The shipped reviewers have no G-series to extend, and adding a `COR-*` equivalent to each would need the re-pin the issue wrongly assumed was needed here — turning a one-file prose fix into four files plus three locks. `starter.md` was also rejected: a concrete rule sits oddly among `{{PREFIX}}` stubs a copier is meant to replace.
- **`flat()` in `check-skill-contracts.py` is carved out, not fixed.** It collapses whitespace on both operands of one comparison so a needle matches across a line wrap. Removing it makes the guard false-fail on rewrapped prose, and a guard that false-fails is a guard someone deletes.
- **The audit table stays in the archived spec; its conclusion moved into the rule.** A dated snapshot of thirteen files that nothing checks would, in `docs/`, certify what it is not checking — `G-8`'s shape applied to a document. The reviewer's suggestion: leave the evidence where evidence goes, promote the one durable sentence to where a reviewer is already reading.
- **Ordinal case citations abandoned repo-wide.** `scripts/test-gates.sh` has two numbering schemes that disagree — 44 numbered source comments whose maximum is 39, against 51 printed results — and the source scheme is not even injective, since `# 14.` through `# 18.` are each used twice. Six citation sites plus `check-templates.py`'s pre-existing `case 32` now cite assertion names.
- **Accepted rather than fixed:** #49's allow-list gap. The interesting fix — deriving the reviewer's permitted commands from `.steering/tech.md` instead of restating them — would make those commands a function of a file the branch under review can edit. That is an escalation needing its own argument, not a drive-by.

### What went wrong on our side, recorded because the pattern is the point
- **Both HIGHs were the rule stating a true principle in a form broader than its evidence.** The first told the reader the clearing side was risk-free — making the one residual hole this repo has *documented* the one the rule instructed a reviewer not to look for. The second granted the whitespace carve-out on "cues live in a line's interior", a property of this repo's current cues rather than of cues.
- **A mechanical fix to citation drift introduced worse citation drift.** Chained `.replace()` calls double-applied and left `assertions's backticked-red` in the sentence stating the audit's conclusion — inside the section about citations being unreliable.
- **The spec's first rejection of a mechanical checker was self-serving and the reviewer said so.** It rejected a scope guaranteed to find nothing; the scope that resolves citations *outward* would have gone red on that very branch. The conclusion survived, the ground for it was rewritten.
- **The reviewer withdrew evidence twice** — its `case 39` claim, and its own "twice a normal rule" length target, which it had produced without checking against a constraint it had insisted on one paragraph earlier. Both times the conclusion held and the reasoning was corrected in the open.

### Next steps
- [ ] Merge #45's PR, then `archive` the spec.
- [ ] #48 needs a human on one point: `README.md`:133 claims the fidelity matrix rows were each produced by running them, against named Claude Code and Antigravity versions. Updating a "we ran this" claim without running it is the defect the line warns about.
- [ ] #47 (all three `rules-lock.json` notes say "Grounding for ts-reviewer") is still untouched and was deliberately avoided on this branch despite being adjacent.
- [ ] Backlog order is unchanged: #26 still top, then #39, #36, #35.
