## 2026-09-20 — Hiroyuki

### Summary

Implemented CI release distribution packaging tool and GitHub Actions workflow to isolate runtime plugin payload from repo-internal development tooling (`gate-sdd.zip`), documented consumption and rejected options, and fixed fail-closed manifest validation on unpacked distribution trees (#140). Version bumped **0.13.0 → 0.14.0**, test-gates 101 → 102 passed across 2 review rounds.

### Activities

- Authored spec `.specs/140-isolate-runtime-plugin-payload/spec.md` (#140) with requirements, clarifications (C1–C4), out-of-scope boundaries, design, and TDD tasks (T1–T4).
- T1 (Red-Green-Refactor):
  - Authored test case 76 in `scripts/test-gates.sh` verifying that `scripts/package-release.py` bundles runtime payload files (`plugin.json`, `.claude-plugin/`, `skills/`, `agents/`, `hooks/`, `rules/`, `assets/`, `AGENTS.md`, `README.md`, `README.ja.md`, `LICENSE`), preserves permissions and symlinks (`rules/AGENTS.md`), strictly excludes internal tooling (`.specs/`, `.steering/`, `scripts/`, `evals/`, `docs/`, `.work_logs/`, `.github/`, `.vscode/`, `.git*`), and fails closed when expected runtime components are missing.
  - Implemented `scripts/package-release.py` using Python 3 stdlib only (`zipfile`), handling Unix symlinks (`S_IFLNK`) and permissions.
  - Verified RED then GREEN on test-gates.
- T2: Created `.github/workflows/release.yml` triggered on release tags (`gate-sdd--v*`, `v*`) and `workflow_dispatch`, building `dist/gate-sdd.zip`, validating extracted manifests, and attaching the archive to GitHub Release.
- T3: Documented distribution artifact consumption in `docs/layout.md` and `docs/fidelity.md`, explicitly contrasting `gate-sdd.zip` against raw `git clone` / local path installations and recording reasons for rejecting Option 1 (`.gitattributes export-ignore`) and Option 2 (`plugins/gate-sdd/` subfolder).
- T4: Verified all 12 validators and test suite pass cleanly; ticked all ACs and tasks in `spec.md`.
- Review Round 1:
  - Invoked `gate-sdd-reviewer` subagent via `define_subagent` and `invoke_subagent`.
  - Reviewer caught BLOCKER: `scripts/check-manifests.py` hardcoded `ROOT` and ignored `sys.argv[1]`, causing CI release validation (`python3 scripts/check-manifests.py /tmp/verify-dist`) and test-gates step 3 to evaluate the repo root rather than the extracted archive, failing open.
  - Fixed `scripts/check-manifests.py` to support target directory argument in `sys.argv[1]`.
  - Fixed `scripts/package-release.py` to resolve relative output path against `pathlib.Path.cwd()`.
  - Added negative test assertions (`c_pkg2b`, `c_pkg2c`) in `scripts/test-gates.sh` asserting `check-manifests.py` fails closed (exit 1) on a corrupted/tampered unpacked directory.
- Review Round 2:
  - Re-invoked `gate-sdd-reviewer` subagent: returned `APPROVE`, `CLEAN`, 0 blockers, 0 high. Recorded receipt in `.specs/140-isolate-runtime-plugin-payload/.review-receipt`.
- Post-review: Bumped version in `plugin.json` and `.claude-plugin/plugin.json` from `0.13.0` to `0.14.0`, set spec `Status: done`, verified with `check-version-bump.py main` (0.13.0 -> 0.14.0) and all 12 validators.

### Decisions

- **Option 3 (CI Release Distribution Artifact `gate-sdd.zip`):** Preserves the foundational architecture ("the repo root IS the plugin", dual-manifest layout, no nested directories) while providing consumers who want payload isolation a clean artifact without repo-internal dev tooling.
- **Rejected Option 1 (`.gitattributes export-ignore`):** Marketplace installations clone the git repository directly, so gitattributes export-ignore rules never apply to the common install path.
- **Rejected Option 2 (`plugins/gate-sdd/` subfolder):** Moving the plugin into a subfolder violates `docs/layout.md`, fractures the dual-manifest layout, and breaks downstream pinned references and git clone installation without templating.
- **Support target directory in `scripts/check-manifests.py`:** Enforces **gates never fail open** during release packaging verification by evaluating the unpacked payload rather than the repository root.
- **Minor version bump (0.14.0):** Introducing standalone release artifact packaging and automated tag-triggered release distribution is a new plugin delivery capability.

### Next steps

- [x] Push branch `140-isolate-runtime-plugin-payload` and open Pull Request for #140 (merged in commit `e6655b0`).

## 2026-09-20 — Antigravity (Hiroyuki)

### Summary

Aligned `README.md` and `README.ja.md` with the Antigravity parity track (#143, #144, #145, #146, #147) and release distribution artifact (`gate-sdd.zip` from #140), updating installation options, context isolation rule pointers, fidelity table rules discovery, and `init` active harness detection (#157). Passed 1 review round cleanly with 0 findings, 102 test cases passed, no version bump required (documentation chore).

### Activities

- 157-align-readme-with-antigravity-parity (#157):
  - Filed GitHub Issue #157: `docs(readme): align README and README.ja.md with Antigravity parity and release packaging`.
  - Authored spec `.specs/157-align-readme-with-antigravity-parity/spec.md` with AC1–AC5, out-of-scope boundaries, design, and TDD-ordered tasks (T1–T4). Verified with `scripts/check-templates.py`.
  - T1: Verified baseline guards pass cleanly (`check-readme-claims.py`, `check-markdown-fences.py`, `test-gates.sh`). Set spec `Status: approved`.
  - T2: Updated `README.md`:
    - Documented both Google Antigravity install options: standalone release archive (`gate-sdd.zip` from GitHub Releases) and direct `git clone`.
    - Clarified context isolation rulebook location prose: `not in AGENTS.md (or CLAUDE.md / GEMINI.md)`.
    - Added `Rules discovery` row to the fidelity comparison table: `root AGENTS.md` vs `rules/AGENTS.md`.
    - Updated `init` skill description to note active harness detection (Claude Code, Antigravity, or both) and corresponding rule pointers/directory configuration.
  - T3: Updated `README.ja.md` in lockstep with matching Japanese translations for all T2 changes.
  - T4: Verified all 12 validators, `scripts/test-gates.sh` (102 passed, 0 failed), `scripts/check-readme-claims.py`, and `scripts/check-markdown-fences.py` pass cleanly. Set spec `Status: done`.
  - Review Gate: Spawned `gate-sdd-reviewer` subagent via `invoke_subagent`. Reviewer independently audited the diff against `main`, evaluated rulebook `rules/claims-and-prose.md`, and returned `APPROVE`, `CLEAN`, 0 blockers, 0 high (`reviewed_by=subagent`).
  - Saved review receipt to `.specs/157-align-readme-with-antigravity-parity/.review-receipt`.

### Decisions

- **Exempt from version bump:** Changes are strictly confined to `README.md`, `README.ja.md`, and `.specs/157-...`. Under `skills/implement/SKILL.md` and `check-version-bump.py`, non-shipped documentation updates do not increment manifest versions.
- **Lockstep English/Japanese parity:** Every claim, code block, table row (`Rules discovery` / `ルール検出`), and note added to `README.md` is identically mirrored in `README.ja.md`, preserving C-3 and passing `check-readme-claims.py`.

### Next steps

- [ ] Push branch `157-align-readme-with-antigravity-parity` and open Pull Request for #157.

