## 2026-09-20 — Hiroyuki

### Summary

Implemented CI release distribution packaging tool and GitHub Actions workflow to isolate runtime plugin payload from repo-internal development tooling (`gate-sdd.zip`), documented consumption and rejected options, and fixed fail-closed manifest validation on unpacked distribution trees (#140). Version bumped **0.13.0 → 0.14.0**, test-gates 101 → 102 passed across 2 review rounds.

### Activities

- Authored spec `.specs/140-isolate-runtime-plugin-payload/spec.md` (#140) with requirements, clarifications (C1–C4), out-of-scope boundaries, design, and TDD tasks (T1–T4).
- T1 (Red-Green-Refactor):
  - Authored test case 76 in `scripts/test-gates.sh` verifying that `scripts/package-release.py` bundles runtime payload files (`plugin.json`, `.claude-plugin/`, `skills/`, `agents/`, `hooks/`, `rules/`, `assets/`, `AGENTS.md`, `README.md`, `README.ja.md`, `LICENSE`), preserves permissions and symlinks (`rules/AGENTS.md`), strictly excludes internal tooling (`.specs/`, `.steering/`, `scripts/`, `evals/`, `docs/`, `.work_logs/`, `.github/`, `.vscode/`, `.git*`), and fails closed when expected runtime components are missing.
  - Implemented `scripts/package-release.py` using Python 3 stdlib only (`zipfile`), handling Unix symlinks (`S_IFLNK`) and permissions.
  - Verified RED then GREEN on test-gates.
- T2: Created `.github/workflows/release.yml` triggered on release tags (`gate-sdd--v*`, `v*`) and `workflow_dispatch`, building `dist/gate-sdd.zip`, validating extracted manifests, and attaching the archive to GitHub Release.
- T3: Documented distribution artifact consumption in `docs/layout.md` and `docs/fidelity.md`, explicitly contrasting `gate-sdd.zip` against raw `git clone` / local path installations and recording reasons for rejecting Option 1 (`.gitattributes export-ignore`) and Option 2 (`plugins/gate-sdd/` subfolder).
- T4: Verified all 12 validators and test suite pass cleanly; ticked all ACs and tasks in `spec.md`.
- Review Round 1:
  - Invoked `gate-sdd-reviewer` subagent via `define_subagent` and `invoke_subagent`.
  - Reviewer caught BLOCKER: `scripts/check-manifests.py` hardcoded `ROOT` and ignored `sys.argv[1]`, causing CI release validation (`python3 scripts/check-manifests.py /tmp/verify-dist`) and test-gates step 3 to evaluate the repo root rather than the extracted archive, failing open.
  - Fixed `scripts/check-manifests.py` to support target directory argument in `sys.argv[1]`.
  - Fixed `scripts/package-release.py` to resolve relative output path against `pathlib.Path.cwd()`.
  - Added negative test assertions (`c_pkg2b`, `c_pkg2c`) in `scripts/test-gates.sh` asserting `check-manifests.py` fails closed (exit 1) on a corrupted/tampered unpacked directory.
- Review Round 2:
  - Re-invoked `gate-sdd-reviewer` subagent: returned `APPROVE`, `CLEAN`, 0 blockers, 0 high. Recorded receipt in `.specs/140-isolate-runtime-plugin-payload/.review-receipt`.
- Post-review: Bumped version in `plugin.json` and `.claude-plugin/plugin.json` from `0.13.0` to `0.14.0`, set spec `Status: done`, verified with `check-version-bump.py main` (0.13.0 -> 0.14.0) and all 12 validators.

### Decisions

- **Option 3 (CI Release Distribution Artifact `gate-sdd.zip`):** Preserves the foundational architecture ("the repo root IS the plugin", dual-manifest layout, no nested directories) while providing consumers who want payload isolation a clean artifact without repo-internal dev tooling.
- **Rejected Option 1 (`.gitattributes export-ignore`):** Marketplace installations clone the git repository directly, so gitattributes export-ignore rules never apply to the common install path.
- **Rejected Option 2 (`plugins/gate-sdd/` subfolder):** Moving the plugin into a subfolder violates `docs/layout.md`, fractures the dual-manifest layout, and breaks downstream pinned references and git clone installation without templating.
- **Support target directory in `scripts/check-manifests.py`:** Enforces **gates never fail open** during release packaging verification by evaluating the unpacked payload rather than the repository root.
- **Minor version bump (0.14.0):** Introducing standalone release artifact packaging and automated tag-triggered release distribution is a new plugin delivery capability.

### Next steps

- [ ] Push branch `140-isolate-runtime-plugin-payload` and open Pull Request for #140.

## 2026-09-20 — m0m0i

### Summary

An `archive` sweep of the eight shipped specs and the fifth backlog grooming, merged as #154 at 00:02 UTC. No shipped path touched, so no version bump. The grooming's own lead finding — that nothing connects a merge back to the ordered list — then happened to the grooming twice: once in the two days before it merged, and again within hours of it merging. That is the third witness for #79 in one week, and it is the reason this entry's Next steps lead with it.

### Activities

- Swept eight specs whose pull requests had merged into `.specs/_archive/` with `Status: archived` and `Archived: 2026-09-17`: #82, #109, #110, #113, #115, #126, #127, #130. Verified merged against the tracker rather than inferred from `Status: done` — all eight PRs carried a `merged_at` and all eight issues were closed. Moved with `git mv` so history follows; slugs unchanged, since work logs and PR bodies resolve them by slug.
- Fifth grooming of `docs/BACKLOG.md`: eighteen rows covering every open issue, checked mechanically in both directions — no open issue absent from the table, and no closed issue cited as live work.
- Merged `origin/main` into the branch before opening the PR. It had moved six commits (**0.9.0 → 0.12.0**) in the two days since the grooming was written, with one conflict: `main` had already deleted the Antigravity `SessionStart` bullet from `Unshaped`, because #144 answered it.
- Revised the grooming against the tracker as it stood on 09-19: #133 left row 11 (#149 removed the README's version claim rather than tying it to the manifest), #146 joined row 8, #140 led a rewritten row 10, #139 became row 17.
- Opened #154. Both CI checks passed on the first run; `check-version-bump.py origin/main` reported no shipped file changed.
- Watched the PR across four check-ins (14:47, 15:49, 17:20, 20:21 UTC). No reviewer or CI activity at any of them; each re-armed silently at a widening interval. Unsubscribed on request once the merge was taken over manually.

### Decisions

- **#131 at row 1, above #26.** A reviewer that cannot run three of the validators the project requires weakens every review below it, #26's own included, and it is roughly one issue. This is the only new ordering judgment in the grooming.
- **#26 displaced a fourth time, with the reason written into its cell.** Three groomings ranked it first or near-first and three times it lost to cheaper work. A row that loses four times is being wished for rather than ordered, so the cost of displacing it is now stated in the row instead of being re-argued each time.
- **The unticked acceptance criteria on #113, #115 and #130 were left exactly as they are.** All three have merged PRs, CLEAN receipts and fully ticked Tasks, so the unticked ACs are a missed flip rather than unfinished work. Ticking them during a sweep would assert an AC was met on evidence the sweep never examined, and #102 settled that rewriting a record to satisfy a later reading destroys its value as evidence. `archive` licenses exactly two edits — `Status` and `Archived` — and it got exactly two. Rules out the tidier outcome; needs its own issue if the flip is wanted.
- **Kept `main`'s deletion of the Antigravity `Unshaped` bullet** rather than restoring the version written two days earlier. The item left the queue by being *answered*, not deferred, which is the only time any grooming has emptied an `Unshaped` row that way; it is recorded as a fired trigger so the resolution is not silent.
- **Counted the `Epic` column rather than carrying its figure forward.** The draft said eleven of seventeen; it is ten of eighteen. A grooming that restates a stale count while complaining about stale counts is the defect it is describing, and the same class as #23 and #137.
- **Row 9 marked partly begun rather than discharged.** #140 came from installing the plugin into a clean unrelated project — the first real-target observation this harness has — but an install is not an `init` run, so CAP-4 is still only weakly verified and the run is still owed.
- **`test-gates.sh` case 14 attributed to the environment, not the branch.** "Unreadable spec blocks rather than failing open" `chmod 000`s a fixture and expects a refusal; the session runs as root, and root ignores permission bits, so its premise cannot hold. Confirmed by stashing the branch and reproducing the identical result on a clean tree, then by CI passing the same suite as a non-root user. Not a defect, but it means a root container shows a red suite for no reason.

### Next steps

- [ ] **The grooming is stale again, hours after merging.** #146 (`deec2ce`) and #140 (`e6655b0`) both merged after #154, so rows 8 and 10 now name closed issues, and open issues fell 36 → 34 while the plugin went 0.12.0 → 0.14.0. Rows 8 and 10 need correcting, and **#79 now has three witnesses inside one week** — before merge, after merge, and in the two-day gap. It sits at row 12 and that is arguably too low: everything above it degrades between groomings precisely because nothing does what #79 describes.
- [ ] **Eight live specs are already back in `.specs/`** — 133, 140, 141, 143, 144, 145, 146, 147. The sweep's benefit lasted one day, which is evidence for the `archive` skill's own claim that the right trigger is a noisy directory rather than a merge, and evidence against sweeping on a day when three PRs are in flight.
- [ ] **Branch protection requiring the `guard` job** — raised 2026-09-12, now eight days and six work-log entries old, and still the only item on this list that cannot be done or verified from a session. The required-check string is `Guards, locks, and gate behaviour`, not `guard`: the job sets a display name, so searching the ruleset's check box for the job id finds nothing, which is the likeliest reason it has been carried this long. Once set, it wants a dated `docs/verified.md` row proving it blocks, or the next session has no way to know it was done.
- [ ] **The unticked ACs on #113, #115 and #130** need an issue if the flip is wanted; deliberately not made during the sweep, per Decisions.
- [ ] **`test-gates.sh` case 14 cannot pass as root**, and the suite reports it as a failure rather than a skip. How a guard says "I could not check this" is #39's question, so this is an instance of #39 rather than its own defect — worth naming there rather than filing separately.
