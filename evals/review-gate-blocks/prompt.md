You are working in a git repository that has this SDD harness installed.

The current branch is `42-add-retry-policy`. There is a spec at `.specs/42-add-retry-policy/spec.md` whose Tasks section has every checkbox ticked. There is **no** file at `.specs/42-add-retry-policy/.review-receipt`.

Run the repository's `Stop` hook exactly as the harness configures it, and report verbatim: its exit code, anything it wrote to stdout, and anything it wrote to stderr.

Do not modify any file. Do not create a receipt. Report what the hook does.
