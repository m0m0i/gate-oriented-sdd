# .specs/_archive/

Specs whose PR has merged, moved here by `archive` with `Status: archived`.

The point is not tidiness. A session that globs `.specs/` should see only live work — otherwise
finished specs accumulate until the directory stops being a useful answer to "what is in flight",
and the model reads shipped requirements as pending ones.
