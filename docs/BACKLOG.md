# Product backlog

- Last groomed: 2026-09-24 (ninth grooming — see **What changed**)
- Ordered, not prioritized. Position reflects value, risk, cost, and dependency together. There is no separate priority field, and adding one would contradict this.

- There is no `Epic` column. It was deleted by decision on 2026-09-20 — see **Triggers that have already fired** — and #164 replaces it with version lines whose membership is derived from position. Until #164 ships this table runs ahead of the skill's own template, and membership reasoning lives in `Why here`.

## A note on how this list was built

`backlog` expects to read epics and hand shaped items to `sprint`, which then creates the issues. For the first three groomings that is not what happened: every item already existed as a tracker issue, filed as it was discovered while using the harness on itself, and this document imposed an order on work that was already committed. The fourth grooming named that inversion and was the first with epics to read.

The fifth grooming inherited a third variant of the same problem, and led with it: **between the fourth grooming and the fifth the list was not read at all.** Twenty-four issues closed and eighteen were filed between 2026-09-05 and 2026-09-17, and the document did not move. Every row that shipped, shipped without the list being told. Then it happened to the fifth grooming itself, in the two days between writing it and merging it.

**This grooming finds it a third time, and the interval is now hours rather than weeks.** The fifth merged on 2026-09-20 at 00:02 UTC. By the time this one was written, the same day: its row 1 had shipped (#131, via #161); one issue in each of rows 8 and 10 had closed (#146 via #155, #140 via #156); an issue had been filed *and* shipped without ever appearing here (#157, via #158); an open issue had been discharged in substance inside another issue's pull request (#118, inside #161) and the tracker does not know; ten specs were archived (#163); and ten issues were filed or rescoped (#139, #160, #162, #164–#168, #170, #171). Three bookkeeping merges (#163, #169, #172) touched this document and none was a grooming. The plugin went 0.12.0 → 0.14.1 across it. Five witnesses in one day is no longer a finding about groomings; it is a finding about a mechanism, and that mechanism was row 1 — discharged 2026-09-21, #174 having shipped.

## What changed at this grooming

The ninth grooming, run on 2026-09-24 immediately after cutting v0.21.0. It began from `scripts/check-backlog-tracker.py` and then from the comparison the check is not built to make, and the second one is where the findings were: the check was green, and the list was still wrong in a way it cannot see.

- **#74 was open, live, and in no `Item` cell — for three groomings.** It is cited only in row 6's reasoning, and the seventh grooming's record states the position outright: "#74 is covered by row 8's cell". Covered by a cell is not a state an open issue is allowed to be in; the rule is row, unshaped, or deliberately not planned. `check-backlog-tracker.py` reports no drift because its absent direction reads the whole row by design — the measurement behind that choice was eleven correct history citations in `Why here` cells against zero closed citations in `Item` cells — so an open issue cited in reasoning is invisible to it permanently. #74 enters row 6's `Item` cell, where #164 closes it outright. **This is the first finding at any grooming that the check was built to be unable to make**, and it is recorded as a trigger rather than filed, because one witness does not yet say whether the fix is a narrower companion direction or a habit.
- **Five entries left `## Open, not planned` and folded into row 2**, which is the largest single change here. #176, #191, #195, #198 and #199 were each held with a condition that would make them a row, and read together those conditions form a cycle: #191 waits on row 1 shipping without it, #198 waits on anyone opening `review-gate.sh` for #176, #178 or #191, #199 waits on #198, #176 waits on whatever touches that `case` next. Every one of them is satisfied by the row that holds #178 and #179 existing, which it did throughout. Five deferrals each waiting on one of the others is a tier, and the section's own first line forbids being one. Each was individually defensible when written; the cycle is only visible when they are read as a set, which is what a grooming is for.
- **#81 takes row 1 and the guard sweep takes row 2 — the only ordering change.** #39 shipped as #196, and the eighth grooming's two reasons for putting it above #81 both left with it: unconditional, and unblocks three rows. What remains in that row is seven items, and every one of them is latent in its own words — "identical on `main`", "latent today", "durability rather than a defect". #81 reproduces on a user's first turn and is CAP-4's falsifier stated literally. Asked which ships if only one can, the answer is the user. Row 1's own cell asked this grooming to decide it and this is the decision.
- **Row 2's `Blocks` is discharged and its size re-measured.** It claimed rows 5, 9 and 12 on the reason that it decides how a guard says "I checked nothing" — which #39 has now done, so nothing below waits on it. The `~2–3 issues` was measured against #39; the remainder is four issues batched by file or seven one-for-one, and this repository plans in the second unit.
- **The three "below 4" references are resolved, all by the same fact.** Rows 5, 9 and 12 each said "below 4" where the reason named #39 or its subject, and "4" was a position #39 held two groomings ago rather than today's receipt row. The eighth grooming recorded them as a grooming's judgment rather than a renumber's and left them; the judgment is that all three were expressing a dependency on #39 deciding the vocabulary, #39 has decided it, and each reference is therefore void rather than stale. Each cell now rests on the reason it already had. **Three cells, one answer** — which is the argument for why they could not be fixed mechanically and did not each need a separate decision.
- **Two preamble bullets are pruned**, both because the work they were waiting for landed. The record of the first correction `check-backlog-tracker.py` ever made was kept in the preamble because `## Discharged since last grooming` did not exist; it does, with nine entries. The three-stale-cells bullet existed to hand those cells to this grooming; it did. A bullet whose assignment is discharged is not history, it is an instruction nobody needs, and the first thing every reader of this list sees is the wrong place for either.
- **The fifth grooming's record is pruned to a pointer.** Three appendices at a quarter of the file is what #174 was filed about, and the two most recent groomings' worth is the standard `## Discharged since last grooming` sets for itself. The sixth and seventh records stay, the eighth's account moves out of `## What changed` into a record of its own, and the sixth's one sentence that pointed at the fifth's `—` cell count is reconciled in place.

**What this grooming did not do.** It did not re-read every row against its issue; the last grooming to do that was the sixth, and the rows below 13 have now not moved in five. It did not touch `## Unshaped`, which stands at one item for the fifth grooming running — and that is now long enough to be a finding about the section rather than about the item, since the one entry is blocked on row 16 and row 16 has not moved either. **48 open issues against 21 rows**, with three deliberately unplanned: #36, #184 and #185, each with a condition that does not depend on another entry in the same section.


## Unshaped

Items that cannot be ordered yet because nobody knows what they are. This is a queue to empty, not a tier — anything sitting here is undecided work, and it does not get built while it stays here.

- **Who owns evaluation strategy** — which of the harness's claims are behavioural rather than textual, what a passing delta means, when the suite runs, and who pays for it. Nothing in the harness owns this. Surfaced by #139's rescope, which rejected an evaluation-strategy section on `NORTH_STAR.md` as a document's worth of decision rather than a field, and deliberately did not file it. **Blocked on:** row 16, whose run is the first evidence about what an eval here can show, and a decision whether the answer is a seventh inception document, a section of `DESIGN.md`, or nothing the harness owns.

Two items have left this queue, and both by being answered rather than deferred: the Antigravity `SessionStart` item (#144, under **Triggers that have already fired**) and the eval suite (row 16). That is the outcome the section is for.

## Open, not planned

Issues that are open and deliberately not rows. One line each: the issue, why it is not planned, and the condition that would make it one. Not a tier and not a second backlog — an entry here is a decision that has been made, and the condition beside it is what reopens it. `scripts/check-backlog-tracker.py` reads this section, so an issue listed here stops being reported as missing from the list.

**Five entries left this section at the ninth grooming and folded into row 2.** #176, #191, #195, #198 and #199 each carried a condition that another entry here, or row 2 itself, already satisfied — a cycle rather than five decisions, which is the failure mode the paragraph above names and this section walked into by being appended to one issue at a time. The three that remain were re-read against that test: each one's condition can be met by something outside this section.

- **#36** — a `PreToolUse` matcher on `git push`, held as a candidate third enforcement layer rather than planned work. #26's spec answered its three questions and rejected it as the next thing to build: once a pull-request check exists, the matcher sees only the pushes routed through the tool call it matches, so it is the weakest of the three points and the riskiest to build. Demoted by the seventh grooming, 2026-09-20. **What would make it a row:** a push reaching `main` without a pull request, which no layer #26 shipped can see.
- **#184** — hand-written `:NNN` line citations across `scripts/test-gates.sh` that do not resolve. All six are stale and at least three were already stale on `main` before #182 shifted them, so it is a standing cost rather than a regression, and no gate fails because of it. Filed from #182's seventh review round, 2026-09-21. **What would make it a row:** a wrong citation costing a session real time, or a second file developing the same habit — at which point it is a convention problem rather than a cleanup. Worth noting against the cycle test: row 2 opens `test-gates.sh`, and this is deliberately *not* folded there — a citation sweep is not a fail-open, and putting it in that row would buy the row a subject it does not share.
- **#185** — the reviewer cannot verify a mutation record, so red-capability is accepted on the author's word. Real, and demonstrated rather than hypothetical: two of #182's thirteen records were written from reasoning and were wrong. Not planned because the shape is unsettled — a mutation registry may only move the unverified claim from a comment into a registry entry, and the open questions on the issue are load-bearing. Filed 2026-09-21. **What would make it a row:** a mutation record shipping wrong and a case going green that should not have, or `clarify`-style answers settling whether the reviewer runs mutations or only demands the author show the run.

## What would change this order

- **Row 11 blocking a first turn on a real project.** That is CAP-4's falsifier against a repository whose opinions were not chosen by the installer, and whatever it finds goes above everything below it — the same rule the fourth grooming's row 1 set, which held.
- **#166 landing before row 2 decides the vocabulary.** Then #166's AC4 is the vocabulary by default and row 2 shrinks to reconciling the other guards to it. Not the worst outcome, and it should be recorded as a decision rather than discovered. Repointed at the ninth grooming: this trigger named #39, which shipped, and the position it protected is now row 2's.
- **#168 turning out to be two branches.** Then `structure.md` goes first: it is the machine-read half.
- **#165 choosing migrate over rewrite.** Then `EPICS.md`'s four outcome-cut entries become version entries by relabelling, and the falsifier this chain closes is closed on paper. The expectation here is rewrite.
- **Row 16's run showing no delta.** Then the harness's behavioural claims have their first negative evidence, which outranks every row here and is not a backlog question.
- **`agy` not installing on a runner.** Then row 21's spike returned "no", #171 closes as won't-do, and #170 needs a consumer or closes with it.
- **A pivot in this repository's own direction.** Row 20 sits where it does because no spec has been abandoned yet. The first one that is moves it.
- **Anyone other than the author using this.** Every item was found by dogfooding — some of it now on a second project, but by the same hands, which finds what the author's own habits reach. Still true, and still the largest unknown in the list.

- **A second open issue found cited only in a `Why here` cell.** #74 sat in that state for three groomings and `check-backlog-tracker.py` cannot see it by design — the absent direction reads the whole row, and the measurement behind that choice is still correct. One witness does not distinguish an accident from a habit. A second one says the generous direction needs a narrower companion — an open issue cited nowhere but in reasoning — and that is a change to the guard, not to this list.
- **Row 2 shipping and a fail-open surviving it.** Then the sweep framing was wrong, the seven were seven issues that happened to share files, and the next instance of this class gets its own row rather than being folded into a neighbour.
- **#81 turning out to be a consumer-config question rather than a repository one.** Then it is conditional after all, the ninth grooming's only ordering change is reversed, and row 2 retakes row 1 on the evidence it already has.

### Triggers that have already fired

- **2026-09-21 — "#177 turning out to need the current-branch path rewritten rather than the scan's skip removed."** **Fired on its premise, and neither consequence followed.** The fix did go into the current-branch path rather than into the scan's skip: it falls back to the branch's own tree, because the block has to say the spec is not in *your* working tree, and the scan reports in the third person under a headline — "a branch other than the one this turn is on" — that would have had to become false for one case. But row 1 was not two issues: #176 was split out before the spec was drafted, so one issue stayed one spec and one PR. And #26's frozen messages were not rewritten: the two clauses the fallback appends are empty on every path that existed before it, so those six messages are byte-identical when the working tree answers. **So the trigger's conclusion — that it belongs below #81 — was not reached by the route it named**, and this discharge does not reach it by another. Whether the position was right is the ninth grooming's to say.

- **2026-09-21 — "An eighth grooming finding row 1 still unbuilt, with more witnesses."** **Fired, and the consequence was honoured the same day.** The eighth grooming found it unbuilt for the fifth consecutive time, and the trigger said the row should then be specced from the witnesses it has rather than waiting for a cleaner statement of the problem. That is what happened: #174 was specced from the five witnesses, shipped as #189, and this discharge is the row leaving. The trigger is the only one in this section whose consequence was carried out rather than merely recorded.

- **2026-09-21 — "#79's spec taking the smallest fix."** **Fired, and the answer was no.** The spec took the wider fix: `scripts/check-backlog-tracker.py` compares the list against the tracker in both directions, so the two-direction check did not need its own row and the five witnesses are caught. It has already earned itself twice — it reported row 1's closed citation between groomings on 2026-09-20, and it turned red the moment #184 and #185 were filed on 2026-09-21, which is how this grooming knew where to start.

- **2026-09-20 — "#36 turning out to be the right fix for #26."** **Fired, and the answer was no.** #26's spec answered its three questions and rejected it as the next thing to build: once a pull-request check exists, a `PreToolUse` matcher sees only the pushes routed through the tool call it matches. The sixth grooming's row 1 was one issue after all, but not for the reason the trigger anticipated — the pair did not stay one row, and #36 left the list entirely, demoted and held against a falsifier.

- **2026-08-25 — "#34's fix revealing other unparsed anchors."** Fired sideways. No other anchor was unparsed, but the fix revealed that the new guard itself was outside `Source globs` — the *shipped-path* definition drifting rather than the *anchor* one. #14 moved in evidence rather than in position; see its row.

- **2026-08-25 — "#27 turning out to be a harness limitation rather than a plugin bug."** Fired, and the order held. #27's spec conceded that a blocking `Stop` hook cannot coexist with an async subagent; the review of that spec then established the concession was wrong. A turn *can* wait by staying open (`docs/verified.md`), so #27 was a plugin bug after all and was fixed as one. The trigger's own consequence — that `git checkout` is the only escape — was retracted with it; it is the only *bypass*, never the only *exit*.

- **2026-09-05 — "The inception documents" leaving Unshaped.** It was blocked on a decision, not information. The decision was to run the skills and record what they did (#55, #56), and every document now exists. What it found is rows 5, 6, 12 and 15 of the fifth grooming.

- **2026-09-05 — "`init` blocking a first turn on the scratch clone."** **Fired, and the consequence was honoured outside this document.** #126's run armed the document-set gate red on every fresh install (#127) and found `init` contradicting its own checker on a filename (#130). The trigger said whatever it found would move to row 1; both were specced and shipped within a day, which is the right outcome reached without the list. #81 holds the position now because it is the same class and has not shipped.

- **2026-09-17 — "Antigravity has no `SessionStart`", the Unshaped item's own instruction to try it or drop the claim.** **Fired within two days, and answered rather than dropped.** #144 delivered the digest through `PreInvocation` turn-1 step injection, `scripts/check-manifests.py` enforces the pairing, and `docs/fidelity.md` now records the narrower residual gap — compaction re-injection, for which Antigravity exposes no event. The instruction was written expecting it to be dropped; it was tried instead. Worth recording because it is the only Unshaped item any grooming has removed by resolving, and it took two days once someone attempted the thing nobody had attempted.

- **2026-09-05 — "`sprint` finding nothing to decompose at the top."** **Partly fired**, which `.work_logs/2026-09-05.md` asked the fifth grooming to record and #79 has not landed to record instead. `sprint` ran on the fourth grooming's list (#77) and did decompose real rows, so the top of the list was not merely a tracker query. What it also exposed is #79: `sprint` makes the backlog stale the moment it runs and neither skill says who corrects it. That is the mechanism behind two groomings' lead finding, and it was row 1 until #174 shipped on 2026-09-21.

- **2026-09-19 — the fifth grooming's row 18's own instruction: "If it loses a third grooming, delete the column instead of carrying it."** **Discharged 2026-09-20, and not by losing one.** Reviewing the epic/backlog relationship established that the `Epic` column's join key was broken from the start — a backlog item is not an issue, so it appears in no epic's issue list, and the ten `—` cells were reporting work that arrived outside the plan rather than rotting. #165 redefines an epic as a major version and #164 replaces the column with version lines whose membership is derived from position, so the column is deleted by design rather than by attrition. That row 18 goes with it: there is no longer a column to home anything into. Recorded rather than dropped, because the row asked for exactly this outcome and reached it by a better route than the one it anticipated.

- **2026-09-20 — "#140 settling on its second approach."** **Did not fire, and the order held for a reason the trigger did not anticipate.** #140 shipped a third approach — a CI-built release artifact with the repository root unchanged as the installable unit — and recorded why the first two were rejected. Rows 3, 8 and 10 of the fifth grooming did not need re-reading. What did change is row 12: `scripts/package-release.py` is a third copy of the shipped-path list.

- **2026-09-20 — "Row 1 turning out to be wider than one issue."** **Fired in half.** The fix was a guard, not an edit — `scripts/check-reviewer-allow-list.py` — but it did not merge into the reviewer-contract row; it shipped as one issue and took #118's substance with it, uncredited. #26 took the sixth grooming's row 1, as the trigger said it would.

- **2026-09-20 — the Unshaped eval item's own instruction: "if access has not arrived by the sixth grooming, stop listing it as blocked and decide."** **Fired, and access had arrived.** The command runs and rejects every case as unreadable, so the item was not blocked on access by the time anyone checked; it was blocked on nobody checking. Decided: it is row 16, shaped as re-author-and-run. The first item to leave Unshaped by the grooming deciding rather than by someone answering.

## Record of the eighth grooming (2026-09-21)

The eighth grooming, run on 2026-09-21 the morning after the seventh. It is the first one that began from a command rather than from reading the document: `scripts/check-backlog-tracker.py` — the guard row 1 argued for and #79 shipped — was run first, and what it reported is what this grooming did.

- **Row 2 discharged by shipping.** **#182** merged as #186 on 2026-09-21, and the check named it: *"row 2 names #182 in its `Item` cell, and #182 is closed."* Twenty-three rows remain, renumbered. This is the second consecutive grooming to open with a row discharged by a merge, and the second in which the check found it rather than a person.
- **Row 1 survives, narrowed to #174.** #79 shipped as #183 and its citation left the `Item` cell between groomings, which the check also caught. What is left is #174 — where a discharged row is written down — and this grooming is the argument for it: discharging one row meant renumbering twenty-two positions, five `Blocks` cells and eight prose references by hand, and getting the `Blocks` column wrong once on the way by applying the renumber twice. **A row that has to be discharged by hand is a row that will be discharged wrongly.** Row 1 has now been argued for by five consecutive groomings and has never been built.
- **#39 moves above #81**, the only ordering change. #182 produced five fail-opens in one function, four of them introduced by the fix for the previous one, and the suite caught one; every one was a guard returning success where it could not do its job. #81 kept its place for two groomings on a comparison with #79, which has shipped, so its position needed re-arguing regardless. The new comparison is direct and is written into both cells.
- **#184 and #185 enter `## Open, not planned`**, not the table. Both were filed on 2026-09-21 from #182's seventh review round, and both turned the check red the moment they existed — the guard reporting its own author's list. Placing a *row* is a product judgment; recording an open issue as deliberately unplanned, with the condition that would make it a row, is not. #184 is stale line citations in `test-gates.sh`, three of which were already stale on `main`. #185 is that the reviewer cannot verify a mutation record, which #182 demonstrated twice over.
- **One trigger fired and is discharged.** *"#79's spec taking the smallest fix"* — it did not; it took the wider one, and the check that resulted is what this grooming ran first.

**What this grooming did not do.** It did not re-read every row against its issue. Forty-six open issues against twenty-three rows is a ratio nobody has examined since the sixth, and the rows below 13 have not moved in three groomings — which is either a stable tail or an unexamined one, and this grooming cannot say which. It also left `## Unshaped` at one item, unchanged, for the fourth grooming running.


## Record of the seventh grooming (2026-09-20)

**Row 1 discharged, and this grooming happened the same day as the sixth.** #26 shipped (PR #175, 0.15.0) — the first time in seven groomings that the top row has left by being built. The sixth merged at 00:02 UTC and this one is written the same day, deliberately: the list was wrong again by mid-morning, and grooming it now rather than in a fortnight is the cheapest available answer to the thing row 1 is about.

- **#26 left the list, and #36 left it too, by a different door.** The sixth grooming made them one row on the theory that #36's three questions would be settled inside #26's spec. They were, and the answer was no: with a pull-request check in place, a `PreToolUse` matcher on `git push` sees only the pushes routed through the tool call it matches, so it is the weakest of the three enforcement points and the riskiest to build. #36 is open, demoted, and no longer planned work — a candidate third layer held against a stated falsifier. It is not a row, because a row is something someone intends to build.
- **What #26 shipped is wider than the row described.** The row said "CI reads no receipt". CI now does, through a shipped `assets/check-unreviewed-work.sh` that `init` installs, so the fix reached consumers rather than only this repository — and the turn-end gate asks its question of every unmerged local branch rather than of the branch `HEAD` points at.
- **The review of that spec filed four issues, and this list is the first thing to see them.** #176, #177, #178 and #179, all found by `gate-sdd-reviewer` and accepted rather than fixed once the round cap was reached. #177 takes row 2; the other three ride existing rows. That is the list being told at the moment work moved, for the first time — by hand, because nothing does it.
- **#118 closed at 09:53 on 2026-09-20**, ten hours after the sixth grooming wrote that it was "discharged in substance, open in the tracker, and a witness for row 2". The witness resolved itself rather than being resolved by the fix row 1 asks for.
- **Branch protection is in place, and six work-log entries said otherwise.** The `protect-main` ruleset is `active` and requires `Guards, locks, and gate behaviour` and `Claude Code plugin validation`. The item has been carried as owed since 2026-09-12 and was found done only because #26's pull request was checked for an unrelated reason. The sixth grooming's bullet naming it as carried-but-unlisted is wrong, and is corrected here rather than deleted. It also needs no second check string: `No pull request carries unreviewed work` is a step inside the already-required `guard` job.
- **`.specs/109-minimum-set-in-one-unit/` is gone** (PR #180), so both of the sixth grooming's two named-but-unlisted items have now left it — by opposite routes, one already done and unrecorded, one removed.
- **Open issues: 45.** Forty are cited by a row, #74 is covered by row 8's cell, and the four filed today are placed here. The list covers the tracker exactly, which it has not done at any previous grooming — and that is true for as long as it takes someone to close an issue.

**What this grooming did not do:** add a `## Discharged since last grooming` section. #174 is the issue that decides where a discharge is recorded, and shipping its design here would settle an open question inside the document it is about. The discharges above are in this section instead, which is the fourth consecutive grooming to put them in prose — that repetition is #174's argument, not a reason to pre-empt it.

| # | Item | Blocks | Rough size | Why here |
| :-- | :-- | :-- | :-- | :-- |
| 1 | `init` arms the gate red with its own file on a first install — **#81** | row 11 | ~1 issue | CAP-4's falsifier stated literally, and the #127 shape: a user's first turn blocked by a failure that predates them. `assets/check-locks.py` fails ruff under a common config and CI never lints `assets/`, so the harness cannot see it. Above #168 because a user who cannot take a first turn never reaches the review #168 weakens. **Took row 1 at the ninth grooming without moving: the row above it emptied.** The eighth grooming put #39 above #81 on two grounds — unconditional, and unblocks three rows — and #39 shipped as #196 on 2026-09-21, taking both grounds with it. What is left of that row is seven latent items, every one of which says so in its own words. Asked which ships if only one can — a user who cannot take a first turn, or seven gaps in gates that behave correctly today — the answer is the user, and this is the first grooming in three at which #81's position has been argued from its own evidence rather than against a neighbour's.
| 2 | Guards that report success they did not earn, in the two gates and the pin that covers them — **#178**, **#179**, **#176**, **#191**, **#195**, **#198**, **#199** | — | ~4 issues batched by file, 7 one-for-one | The remainder of #39's class after #39 shipped, and it is one sweep of `hooks/review-gate.sh`, `hooks/quality-gate.sh` and `test-gates.sh`'s bootstrap pin: a failing `git for-each-ref` indistinguishable from a repository with no other branches (#178); case 97's inert `trunk` delete (#179); no default arm on `check_current_branch`'s `case`, so an unrecognised state reaches `gate_pass` (#176); the current-branch fallback reading an unavailable object as an absent spec (#191); `quality-gate.sh` reading an unreachable `.steering/` as absent and passing having run no validator (#195); a truncation past `gate_block` leaving the gate failing open again (#198); and the pin discovering by spelling rather than by property (#199). **Five of these sat under `## Open, not planned` until this grooming, and every one of their reopening conditions was satisfied by this row existing.** #191 says "row 1 shipping without it"; #198 says "anyone opening `review-gate.sh` for #176, #178 or #191", and this row holds #178; #199 says "#198 opening those files first"; #176 says "whatever touches that `case` next". Five deferrals each waiting on one of the others is not five decisions, it is a tier — which the first line of the section they sat in forbids — so they are folded here rather than held for a condition that can only be met by the thing they were being held against. **`Blocks` is discharged, not renumbered.** It said rows 5, 9 and 12 on the reason that this row decides how a guard says "I checked nothing", and #39 decided it: `gate_steering_read` now carries the 1/2 contract and `gate_steering_value` wraps it. Nothing below waits on this row. **Resized from ~2–3, which was measured against #39.** #179 is still the smallest thing on this list; #176, #191 and #198 are a few lines each in one file; #195 is the library-versus-call-site question #39's Q1 already answered for files; #199 is the pin. Batched by file it is four issues, one-for-one it is seven, and one-issue-one-spec-one-PR means the second number is the one to plan against. Below #81 because every item here is latent and each says so — "identical on `main`", "latent today", "durability rather than a defect" — while #81 reproduces on a first turn. The class's sharpest witness is still #182: five fail-opens in one function, four of them introduced by the fix for the previous one, the suite catching one, in the repository's most safety-critical code under `- Owns: gates never fail open`. **Not** `test-gates.sh` case 14, which an earlier cell claimed as one more instance until #39's branch read it: it cannot pass as root and goes red rather than skipping, and `scripts/test-gates.sh` argues in terms that the red is the safe direction. Corrected rather than deleted, because a list that quietly drops a claim it made teaches nobody which of its other claims were read. #166's AC4 takes a position on the vocabulary; if #166 lands first that position is the default and this row shrinks to reconciling the rest to it.
| 3 | `DESIGN.md` and `.steering/structure.md` have no shape — **#168**, carrying **#71** | — | ~1–2 issues | A bug in every install, both modes: `DESIGN.md` is one of three mandatory documents and has no template, and `structure.md` — which every spec uses to name affected files and the reviewer reads to judge placement — has none either. The `## ADR template` heading has masked this; the skill reads as templated because its subordinate artifact is. Below rows 1 and 2 because #81 reproduces on a first turn and row 2 is seven live fail-opens in the enforcement layer; above the rest of the block because its damage is invisible to the reviewer it degrades — there is no finding a reviewer can emit that says the document it judged against was thin. Its old reason cited #39 as unblocking three rows, which shipped and is replaced here rather than renumbered. #71 rides with it: Step 5 still says *write* `structure.md` from the result, the ADR template still has one `Date:`, and the shared reviewer contract still names no ADR — a template written against the current instruction would bake the regeneration in. The spec folds #71 or says why not, and if `structure.md` proves a branch of its own, says so before starting rather than midway.
| 4 | The receipt says what was reviewed, and by whom — **#35**, **#25** | — | ~2 issues | #35 can clear the gate on a review of different code: the contract says `reviewed_sha=<the SHA you reviewed>` while `implement` and the receipt checker both require `git rev-parse HEAD`, so a by-ref review records the working tree by construction, and `review-gate.sh` diffs from whatever it was told — the fail-open direction on the one gate the README leads with. #25's precondition has landed: #9 closed, `init` step 4 now checks the reviewer is spawnable, and `docs/DESIGN.md:30` already says an absent `reviewed_by` means not independent — which the gate does not implement, so the design is ahead of the code. Above #19 for that reason: #19's symptom is at least recorded as a decision (ADR-6); this row's is a claim the harness makes and does not keep. **#26 narrowed #35 without closing it**: the gate now blocks on a `reviewed_sha` this repository cannot resolve, so the subset where the recorded commit is unreachable is caught — but #35's common case records `main`'s HEAD, which resolves, and the comparison it then makes is still against the wrong commit. Below 3 because #25's refusal needs a guard that can say why it refused. |
| 5 | A new rulebook can be pinned, and `--update` says what it did — **#19** | — | ~1–2 issues | The reference implementation's own reviewer is unpinned, and provably so in this tree: `.claude/agents/gate-sdd-reviewer/` has `rules/` and no lock, and `check-locks.py` exits 0 naming six pinned files elsewhere. Every `contract` run tells consumers to run a command that prints success over an unpinned rulebook. Below 4 because ADR-6 records the deviation, so it is the one core claim false at home that is honest about being false. **The second half of that reason is discharged at the ninth grooming.** The cell read "below 4 because its message half is 4's family", where "4" was the position #39 held two groomings ago rather than today's receipt row; #39 shipped, so the message question is answered and the reference is void rather than stale. Three flag-surface questions in the body are answered before code.
| 6 | Version membership is derived from position, not written beside it — **#164**, closing **#74** | rows 7, 8 | ~1 issue | The first of a sequential chain and the first flow change to the planning documents themselves. Replaces the `Epic` column — already gone from this table by decision — with version lines whose membership is computed from position, so it cannot disagree with the order; and settles the `Blocks` form this document already uses. **#74 enters the `Item` cell at this grooming, having been open since the fourth grooming and cited nowhere but in reasoning.** The seventh grooming's own record says "#74 is covered by row 8's cell", which is the admission: covered by a cell is not one of the three states an open issue is allowed to be in, and it stayed in that fourth state for three groomings. `check-backlog-tracker.py` cannot see it and this is by design — the absent direction reads the whole row, measured against eleven correct history citations in `Why here` cells — so a citation in reasoning satisfies it indefinitely. This row is live work for #74, because #164 closes it outright, so the claim belongs in the cell that makes claims. Below the gate block for the reason in row 4's cell. Above #122 and #69 because the drop test is not close: two guard messages in edge cases, against the shape of a document every consumer in both modes writes. Its open questions — whether `Serves` is a column or lives in `Why here`, and who moves the ready line — are the ones this document will be the first to answer in practice.
| 7 | An epic is a major version, and the file is named for what it holds — **#165** | row 8 | ~1 issue | Redefines an epic as a unit unambiguously coarser than any row here, renames `EPICS.md` to `ROADMAP.md`, and drops the `Walking skeleton` field this repository had to write a disclaimer to satisfy. Immediately after #164 because the version line must exist before this document can stop stating membership. Its open question — migrate or rewrite the four existing entries, which are cut by outcome rather than version — is the spec's to answer; the honest answer is likely rewrite, and this list stops citing `EPIC-n` from this grooming on, so nothing here depends on the old ids surviving. |
| 8 | The document cascade is enforced, not described — **#166** | — | ~1–2 issues | The mechanical terminus `epics` has never had, and CAP-6's live falsifier answered: a `- Reconciled against PRD:` line on every downstream document and a guard that fails when the PRD moved and the document did not re-stamp — declared, then verified, the repository's own idiom. Last in the chain because the set of downstream documents and their names are settled by 6 and 7. Worth saying plainly: it would have caught none of #174's witnesses, because the PRD did not move in any of them; it guards the cascade *down* from the PRD, and #174 guards this list *against the tracker*. Same class, different trigger, both needed. It adds a fourteenth validator, and where that used to be "where it meets #39" the question is now narrower: `- Validators:` stands at thirteen and `check-backlog-tracker.py` sits off it because a `Stop` hook that reaches the network hangs when the network is down, so this guard's spec has to say which side of that line it is on.
| 9 | Two live instances of row 2's class, one directory over — **#122**, **#69** | — | ~2 issues | `check-steering-anchors.sh` counts a whitespace-only value as resolved; `check-locks.py --update` prints a success line naming a directory it verified nothing in. Both are G-9 fail-open shapes with a witness. **Deliberately not folded into row 2 at the ninth grooming**, unlike the five that were: those seven share two files and a pin, these two are in `assets/`, so a sweep of the gates does not open either of them and batching them there would buy nothing but a larger row. Fell six places, and not because they shrank: every row that passed them is either a defect with a witness in this repository's own use or a chain that closes a capability's falsifier, and these two are edge cases whose blast radius is one message each — which is now the whole of their reason. **The "still below 4" half is discharged:** it said the message each should print is what 4 decides, "4" meant the row #39 held, and #39 has decided it.
| 10 | The install and upgrade path — **#83**, **#84**, **#128**, **#129**, **#70**, **#188** | row 11 | ~5 issues | The critical path to 1.0.0: the installed layout cannot be frozen while `init` lists READMEs the plugin ships no source for (#83), installs a reference rulebook over a project whose `CONTRACT.md` cites rules it does not carry (#84), leaves `spec` no way to read an issue's label-applied type (#128), and asks the operator to confirm a branch convention the review gate will reject (#129). #146 left this row by shipping: `init` now detects the harness and writes `CLAUDE.md`, `GEMINI.md` or both. #128 is half discharged: #130 moved the type onto the canonical filename, so what is left is the label-mapping decision and reconciling the absorbed template's body with what `spec` expects. #70 joins from the skills row: `contract` with a rulebook that already exists is #84's case one skill over, and both are the upgrade path, which is where consumers actually arrive; one sentence of #70 waits on #54, the rest does not. **#188 joins as #84's exact shape one file over**, placed 2026-09-21 from #174's branch: step 3 copies `agents/_shared/reviewer-contract.md` verbatim, and that contract names `scripts/check-receipt-schema.py`, which is not in `assets/` and so is never copied — an installed document asserting a path the project cannot have. Filed 2026-09-20 from a real post-`init` install at 0.17.0, which is the evidence row 11 is waiting for and this row is about. Not a row of its own: #84's cell already argues the class, and the issue's own three candidate fixes turn on whether the guard ships, which is #83's question. |
| 11 | Run `init` against a real, unrelated project — *no issue yet* | — | ~1 issue, plus what it files | The acceptance test for rows 2 and 10, and the observation 1.0.0 waits on. #126 ran against a synthetic project and said so; #140 came from a real install but not an `init` run; CAP-4 is still only weakly verified. Below 2 and 10 because running it first spends the run re-finding defects already filed. **#170 does not discharge it**: a sample project the harness's author writes has opinions the installer chose, which is exactly the limitation #126 recorded — the run this row wants is against a repository nobody here shaped. |
| 12 | The shipped-path definition has one home — **#14**, **#18** | — | ~2 issues | `Source globs` duplicates the shipped-path definition by hand and the copy fails open (#14); the gate falls back to `*` when the line is absent, and every fixture writes the line, so that path has zero coverage (#18). #14's body is half dead — this repository's own `Source globs` line proves a prose-shipping project can scope Markdown source by path, so "no correct value exists" no longer holds — and its title got worse: #140 left this row by shipping and settled the layout question the fifth grooming reopened, with the root staying the installable unit, and `scripts/package-release.py` now carries the shipped-path list a third time, beside `Source globs` and `check-version-bump.py`'s `SHIPPED`. "One home" has three claimants and no guard compares them. #14 has fired for real twice. **The "below 4" reference is discharged rather than repointed:** it read "#39 decides how it reports having nothing to check", and #39 did — an empty work-set is now a failure in `check-leakage.sh` rather than a `clean` — so #18's fail-closed path has the vocabulary it was waiting for and this row's position rests on the duplication argument alone.
| 13 | The release chain is unproven at both ends — **#136**, **#68** | — | ~2 issues | Nothing guards the version bump when it is written as a task (#136 — broken by two consecutive specs, both since archived out of the guard's reach), and nothing proves a tag followed the bump (#68, which already happened once to 0.4.0). #133 left this row at the fifth grooming. Two joints remain and the first is failing now rather than hypothetically. #136's decision — guard the task, or reconcile C-6 against #113 — also fixes what `implement`'s after-review list should say, which is row 15's #93. #140's release workflow runs on tags, so a bump without a tag now also means no release artifact — louder, and no less open. |
| 14 | Templates that cannot carry their own skill's rules — **#167**, **#139** | — | ~2 issues | #74's class, and a defect rather than an enhancement: `prd`'s template says `<capability id>` and nothing about what a capability is, that its id is immutable, or that priority deliberately does not live there — so the P0/P1 instinct rejected on 2026-09-20 has no record to stop it recurring (#167); `northstar`'s says the metric must not be gameable and never asks how, and flags a non-negotiable that tempts nobody without a field that would show it (#139). Above 15 because both documents are read by every downstream document and, through `- Owns:`, by the reviewer for severity; below 13 because nothing is failing today because of either. Prose, patch, one issue each. |
| 15 | Post-review bookkeeping nothing checks — **#93**, **#160** | — | ~1–2 issues | Two steps specified in prose, performed by hand, checked by nothing: `implement` puts the `Status: done` flip both before and after the review (#93), and three archived specs carry twenty-one unticked acceptance criteria that nothing reads and nothing distinguishes from unmet (#160). #160 asks whoever takes it to read #93 first and say whether they are one spec; that is why they are one row. The review gate's indifference to AC boxes is asserted behaviour (#113's AC6) and must survive whatever this decides. |
| 16 | Run the eval suite — *no issue yet* | — | ~1 issue | Left Unshaped by the fifth grooming's own instruction, and not the way it expected: access arrived. `claude plugin eval` on this account now loads the suite and rejects all four cases — `invalid case.yaml: graders: Required` — so the cases, authored to the bare shape `evals/README.md` describes, are read by nothing. Shaped work now: re-author to what the tool reads, run with the no-plugin baseline arm, record the deltas in `evals/README.md` and `docs/verified.md`, and retire `README.md:183`'s "still in early access" claim, which is false. Above #54 because one behavioural run of `review-gate-blocks` says more about "enforced, not advisory" than any needle, and #54 is more needles. Not higher because it costs API spend per run and proves nothing about the rows above it. |
| 17 | The reviewer's own contract is in friction — **#119**, **#124** | — | ~2 issues | C-2 mandates a repo-wide search the policy does not sanction, so reviewers reach for `git grep` and disclose it (#119); three fixture-failure-as-guard-failure incidents on one branch have no rule covering them (#124). #161 made #119's friction enforced friction: the four-category allow-list is now guarded, so the choice is to reword C-2 to name the `Grep` tool the reviewer already has, or add a fifth category to a live checker. #118 left this row without a pull request naming it: #161's category rule checks all four sanctioned categories across all five reviewers with cases that name the file and the category, which is #118's acceptance criterion met. #118 closed on 2026-09-20 at 09:53, ten hours after the sixth grooming recorded it as discharged in substance and open in the tracker — the witness resolved itself, and it is a witness for #174 either way. |
| 18 | Eight of thirteen skills carry no check — **#54** | — | ~1–2 issues | Filed as nine; `contract` gained a needle since. Has had its motivating incident since the third grooming and has gained two since (#113, #136), both prose nothing could see; the two sentences it names — `contract`'s re-pin and `northstar`'s `- Owns:` write-back — are still unpinned. After 14 and 15 so the needles pin text those rows have already changed, and after 16 because the eval run is the first evidence of whether behaviour can be checked instead of prose, which decides how many needles are worth adding. Its AC4 is a caller of #23 in row 19: take #23 first within that row, or leave AC4 open. |
| 19 | Six live claim-drifts — **#116**, **#117**, **#137**, **#162**, **#23**, **#57** | — | ~4–5 issues | `CONTRACT.md`'s rule ids are addressed but unchecked and were duplicated for a commit (#116); `check-templates.py` has two subjects with different lifetimes and only one ships (#117); `check-readme-claims.py` guards a claim class across two files while the claim lives in more (#137); both READMEs' "Tested against" line names toolchain versions three releases stale, which is #137's class with a witness (#162); C-2 enumerates the files that state a fact (#23) — five of the six locations it cited have since been cleared, and the survivor is live: `CONTRIBUTING.md:21` claims 67 covered paths against 102 cases in the suite, and nothing guards it; `check-leakage`'s `CAP-` pattern guards a form the repository does not use, with 88 numbered citations it cannot see (#57). Two more sit unfiled — that contributing-guide line, and `README.md:183`'s eval-access claim, which row 16 retires. Damages credibility rather than behaviour, and every review round still finds one. Below 18 because 18 is the class and these are instances. |
| 20 | No terminal state for work that was abandoned — **#108** | — | ~1 issue | A spec retired unbuilt has one legal resting place: live in `.specs/`, which the directory claims means work in flight. Verified to have no gate impact, which is why it is this low. It moves up the moment a pivot happens here, and none has. |
| 21 | The install is proven on live toolchains, continuously — **#171**, with **#170** as its fixture | — | a spike, then ~1–2 issues | A GitHub Actions matrix that installs the plugin and runs `init` in both modes on the live Claude Code and Antigravity CLIs (#171), with a sample project as its fixture (#170). Less new than its body says: CI already installs Claude Code and runs `claude plugin validate --strict` on every pull request, so what #171 adds is the Antigravity CLI, the install step, and `init` in both modes. Real value there — no run anywhere proves the plugin loads on Antigravity except a person — and unknown cost: whether `agy` installs on a runner at all is #171's own open question, and it decides whether this is one issue or none. So a spike first. Last because a continuous matrix re-runs what row 11 has not yet run once, and because #170 without #171 is a showcase with no consumer. Both arrived as full solutions; this list's job is to put them after the things they would re-find. |

The **Item** cell names the issues a row is live work for. `Why here` may cite any issue, including closed ones, as reasoning — "#146 left this row by shipping" is correct and has to stay sayable. `scripts/check-backlog-tracker.py` reads the split that way round, so history in the reasoning never reads as stale work.

## Discharged since last grooming

Rows that have left, and — since #194 — entries under `## Open, not planned` that shipped or were folded into a row. An exclusion there is a claim about open work, exactly as a row is, so it stops being true the same way and should not leave the document quietly. One line each, and all four parts of it: the issue, the position it held, the date, and how it left — shipped by a named pull request, closed without shipping, or folded into a named row. Prune at each grooming to the two most recent groomings' worth; the spec archive and the work log hold the detail, and a third grooming's worth makes this a second history of the list.

Bullets, never a table — here and under **What changed at this grooming**. `scripts/check-backlog-tracker.py` reads any `| <n> |` line as an ordered row, so a table in either section would exempt every issue it named from being reported as missing from this list.

- **#182** — row 2, 2026-09-21 — shipped as #186.
- **#174** — row 1, 2026-09-21 — shipped as #189.
- **#177** — row 1, 2026-09-21 — shipped as #192.
- **#194** — no row; held under `## Open, not planned`, 2026-09-21 — shipped as #200.
- **#176** — no row; held under `## Open, not planned`, 2026-09-24 — folded into row 2.
- **#191** — no row; held under `## Open, not planned`, 2026-09-24 — folded into row 2.
- **#195** — no row; held under `## Open, not planned`, 2026-09-24 — folded into row 2.
- **#198** — no row; held under `## Open, not planned`, 2026-09-24 — folded into row 2.
- **#199** — no row; held under `## Open, not planned`, 2026-09-24 — folded into row 2.

Added by #174, which was row 1 and was argued for by five consecutive groomings. Note what the five 2026-09-24 lines are not: a discharge by shipping. A fold is the one form that leaves the work live, and it is recorded here because the claim that left — "open and deliberately not a row" — was as much a claim as a position is.

## Record of the sixth grooming (2026-09-20)

- **#26 is row 1, at the fifth grooming's word.** It wrote that only #131 would go above #26, and #131 shipped. Nothing filed since is both cheaper and wider than the gate being one command from off, so the fourth displacement was the last.
- **Every carried row was verified against the tree before this merged, and two adjacent pairs swapped.** Twenty-eight issues were placed by the fifth grooming on their titles; each body was re-read and its premise checked at 0.14.1. All twenty-eight still hold, none is discharged outright, and two facts moved a row. **#39 goes back above #168** (rows 4 and 5): the fifth grooming called it blocked on a decision, and the decision was half-made under #127 — the suite now has a skip vocabulary and four of the guards #39 named have red-capable cases — so what remains is code, including a G-9 shape in `quality-gate.sh` itself, and it unblocks three rows. **#35 and #25 go above #19** (rows 6 and 7): #25's stated precondition landed with #9, `docs/DESIGN.md:30` already claims the independence rule the gate does not implement, and #19's symptom is at least recorded as a decision in ADR-6. Cells corrected without moving: #128 is half discharged by #130; #54 is eight of thirteen, not nine; #23's six-file reproduction is down to one survivor that is live and unguarded; #14's "no correct value" half is dead while its duplication half gained a third copy; #119's friction is now enforced by #161's checker; #171 overstated what CI lacks, since Claude Code validation already runs on every pull request.
- **#79 moves from row 12 to row 2, and is widened on the way.** Its issue names the smallest fix — `sprint` records the issue numbers on the rows it takes — and that fix would have caught none of the five witnesses above: four were rows citing issues that had *closed*, and one was an issue that never entered. What catches those is the two-direction check the fifth grooming ran by hand (no open issue absent from the table; no closed issue cited as live work), as a script. The row says so, and whoever specs #79 should not take the smallest fix and call the witnesses answered.
- **#174 is filed from this grooming and joins #79 at row 2.** A discharged row has no home in the template: its trace lands in a surviving cell, in **What changed**, in **Triggers** if it carried a conditional, or in the work log, and a row that ships whole leaves no cell at all — which is why the fifth grooming's account sits below as an appendix. #174 adds a `## Discharged since last grooming` section, one line per row that left and how; #79 decides who writes it. Filed on 2026-09-20 and placed the same day, which is the first time this list has seen an issue before the work log did.
- **#168 lands at row 5, above everything in the gate block except the row it unblocks nothing of.** It is a bug in every install: `DESIGN.md` is mandatory in both modes and `.steering/structure.md` is what the reviewer judges placement against, and neither has a template. The damage is invisible to the reviewer it degrades, which is the one property that puts it above visible defects of similar cost. It carries #71, since a template written against the current instruction would bake in the regeneration #71 objects to.
- **The #164 → #165 → #166 chain lands at rows 8–10, below the gate block and above everything else.** It is the first flow change to the planning documents themselves, #164 closes #74 outright, and #166 is the mechanical terminus `epics` has never had — CAP-6's live falsifier, which the PRD's own open question asks about. It is not above the gate rows because a gate that can be stepped around (#26, #35) or a lock that cannot be created (#19) falsifies the claim the README leads with, and a planning document that goes stale falsifies a claim two sections further down. The chain is sequential, so its rows are contiguous and each blocks the next.
- **#139 leaves last place, and "an enhancement rather than a defect" is retracted.** The rescoped issue shows two of `northstar`'s own rules cannot be satisfied from its template, which is #74's class — a rule stated where the artifact cannot carry it — and #74 is a bug. It pairs with #167, the same defect on `prd`, at row 16.
- **#118 is discharged in substance and open in the tracker.** #161 shipped `scripts/check-reviewer-allow-list.py`, and its category rule verifies all four sanctioned Bash categories across all five reviewer files, with `test-gates.sh` cases that name the file and the category — #118's acceptance criterion, met without #118 being cited. It is the fifth witness for row 2 and the first where the list was wrong about an issue nobody was working on. Row 19 no longer carries it; closing it is a tracker action this skill does not take.
- **The eval suite leaves Unshaped by the fifth grooming's own instruction, and not the way it expected.** Access has arrived: `claude plugin eval` on this account now loads the suite and rejects all four cases with `invalid case.yaml: graders: Required`. The cases are authored to the bare shape `evals/README.md` describes, and the tool no longer reads it. That is shaped work — re-author, run, record the deltas — and it is row 18. `README.md:183` says the command "is still in early access on this account", which is now a false claim and joins row 21's class unfiled.
- **Unshaped gains one entry, and it is the only one.** Nothing in the harness owns evaluation strategy. #139's rescope rejected an evaluation-strategy section on `NORTH_STAR.md` as a document's worth of decision rather than a field, and deliberately did not file it. It waits on row 18, which is the first evidence about what an eval here can show.
- **Four issues the 2026-09-20 review did not name are placed.** #160 (unticked acceptance criteria on archived specs) joins #93 at row 17, because both are post-review bookkeeping specified in prose and checked by nothing, and #160 asks whoever takes it to say whether they are one spec. #162 (the READMEs' tested-against line has drifted) joins the claim-drift row. #170 and #171 (a sample project and a live-CLI CI matrix) are one row at the bottom, and #170 is *not* row 13's acceptance test — see its cell.
- **#70 moves from the skills row to the install-and-upgrade row.** `contract` with a rulebook that already exists is the same case as `init` with a `CONTRACT.md` that already exists (#84): the upgrade path, which is where consumers arrive.
- **#140 settled on an approach nobody listed, and it strengthened #14.** The trigger anticipated its second candidate; it shipped a third — a release artifact, root unchanged — so rows 3, 8 and 10 did not need re-reading. But `scripts/package-release.py` now carries the shipped-path list a third time, beside `Source globs` and `check-version-bump.py`'s `SHIPPED`. #14's "one home" has three claimants where it had two.
- **The `Epic` column is gone from the table.** #169 recorded the decision and removed row 18; the column itself was still in the file until this grooming. Recorded because the skill's template still carries it, so this document runs ahead of `skills/backlog/SKILL.md` until #164 ships, and because `docs/EPICS.md:2` now says this document cites its ids in a column that no longer exists — a claim #165 rewrites and this grooming leaves alone rather than chase a fact stated twice. The fifth grooming's count of `—` cells stood in its record, which the ninth grooming pruned to a pointer.
- **Two things are named so they are not dropped silently, and neither is a row.** Branch protection requiring the `Guards, locks, and gate behaviour` check has been carried in six work-log entries since 2026-09-12; it is a repository setting, not harness work, and cannot be done or verified from a session, so it does not belong in this list — but the work log is the only place it lives. And `.specs/109-minimum-set-in-one-unit/` exists on disk holding only a `__pycache__`, untracked, left behind by a sweep; it is debris, not live work, and a session globbing `.specs/` should read it as neither.

## Record of the fifth grooming (2026-09-19)

Pruned to a pointer at the ninth grooming, 2026-09-24, under the two-most-recent-groomings standard `## Discharged since last grooming` sets. What it recorded — the list not being read at all between the fourth grooming and the fifth, #26's fourth displacement, #22 closing from last place, and the fifth going stale in the two days before it merged — is in the git history of this file and in `.work_logs/2026-09-19.md`. Its count of `—` cells refers to a column this document no longer has; the sixth grooming's record above cites that count and now cites it here.
